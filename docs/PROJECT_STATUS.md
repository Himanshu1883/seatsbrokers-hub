# Project Status
(Living doc — update after every session, read first in every new one.)

Last updated: 2026-08-25 — **Hero 4-up under left CTAs; 7-rail overlap fixed.** Highlights are a 2×2 under Book a Demo / Become a Seller in the copy column. Discover→Settle rail is a 7-col grid with arrows in the gap (Connect/Distribute no longer collide). Previous: **Homepage hero: 4-up + 7-step rail; stadium lightened.** One marketing CTA contract (--primary, var(--radius), shared lift hover). Previous: **TwoTrack identity cards equal height.** Desktop stage stretch + id cards flex:1 + CTA margin-top:auto. Previous: **TwoTrack identity cards horizontal anatomy.** Icon well LEFT (~3.75rem) + text column RIGHT: accent role label (“I'm a **ticket broker**” / “I'm a **B2B partner**”) → separate bold headline → clearer body → pill CTA. Broker mint (`--primary` / `--primary-deep`); B2B slate (`--tt-partner` / `--tt-partner-deep`) — no purple. Faint dotted globe deco bottom-right. Consoles / `--tt-desk-h` / intro L/R unchanged. Previous: **TwoTrack intro L/R split + identity card type.** Intro matches ProcessBento/Stats wallet header. Previous: **TwoTrack intro + identity cards enlarged.** Section heads match ProcessBento/MarketIntelligence scale; dropped intro `max-height: 10svh` / identity crush. Previous: **TwoTrack fixed-desk ≤100svh redesign.** Both consoles share `--tt-desk-h: clamp(280px, 52svh, 420px)` (≤780px height: `clamp(240px, 48svh, 340px)`); stage no longer `flex:1` grows desks. Dense bento internals; identity cards below (~12–14svh); section `100svh` at ≥1024; mobile auto height; page scroll unlocked. Previous: **Book a Demo modal single-column stack.** `data-layout="stack"`: top bar → mint SeatsBrokers title + USP + icon form + product cards → Send message. Panel ≤100svh; only `.demo-modal-body` thin-scrolls. Previous: **TwoTrack desktop 100svh lock + denser desks** (flex-grow desks — superseded). Previous: **TwoTrack metric/identity overlap fixed.** ConsoleShell desks content-size (`max-height: none`); Multi/Live/24/7/AI and PDF/Live/One stay in-flow inside chrome; identity cards sit below with clear column gap (no paint-through). Dense dual-journey redesign retained. Previous: **Book a Demo modal reference light split.** Portrait card (`data-layout="ref"`): surface left rail (Book a demo eyebrow, mint SeatsBrokers title, 4 reassurance rows, SVG deco) + right icon form (fields + 2-col moduleList/API chips + Tell us more); mint Send message; lock privacy; Prefer `/book-demo`. Panel ≤100svh; left never scrolls; right thin-scroll only if short. Previous: **Homepage TwoTrack locked to dense dual-journey redesign** (fc2654dd). ~100svh `min-height` at ≥1024 (height auto), mint broker + blue-gray B2B Demo desks, identity cards under columns, no equal-zoom / hard 100svh crush, page scroll unlocked. Previous: TwoTrack 100svh equal dual-console layout. Previous: **Book a Demo open modal for testing.** `DemoModalProvider` in PageShell; Nav (desktop + mobile), FinalCTA, and homepage Hero open a dark mint `.demo-modal-*` dialog (sentence-case eyebrow, SeatsBrokers title, name/email required, phone/company optional, business-type select from `demoRoles`, multi-select module chips from `moduleList`, `submitLead` handoff, privacy note, Prefer full page → `/book-demo`). Escape / backdrop / X + focus trap + body scroll lock. `/book-demo` route and page unchanged. Previous: 2026-08-25 — **Homepage TwoTrack scroll unlocked + identity cards centered.** Section is page-scrollable (`min-height: 100svh`, `height: auto`, `overflow: visible`; dropped `.lc-body` `overflow: auto` / `overscroll-behavior: contain` and section `overflow-x-clip`). Identity cards moved to a centered `.two-track-id-row` (icon left, colored eyebrow, bold title, gray body, solid CTA, map deco; mint / blue-gray — not purple). Previous: 2026-08-25 — **Homepage TwoTrack (`#platform-tracks`) redesigned to Two Journeys.** Dense Demo desks + identity cards. Previous: 2026-08-25 — **ProcessBento (#partner-process) 01–07 flow is live.** Discover→Settle auto-advances while in view (~2100ms packet hop per join; Settle dwells then wraps). Active card mint border + soft wash; prior steps done (subtler mint); click jumps + restarts dwell; hover/press pause + 450ms resume; `prefers-reduced-motion` freezes; mobile scrolls active card into view. Top split + footer CTA unchanged. Append `.process-bento-step-packet` / `data-state` CSS. Previous: 2026-08-25 — **Homepage Market Intelligence (`#market-intelligence`) matches dense dark mock.** Left copy: **Better Data** mint accent, qualitative live-signals status (no 4.2M+), four feature icon rows, Explore SeatsIntel™ / SeatsPulse™ CTAs. Right: dark Demo `ConsoleShell` with icon rail, Live + range/Filters toolbar, 5 KPIs + demand/confidence gauge, price trend, inventory heatmap, AI recs, market comparison, demand forecast, top movers (illustrative desk £/rows Demo-stamped). Append-only `.mihp-*` dark overrides in `styles.css`. Product-page `.mi-*` untouched. Previous: 2026-08-25 — **ProcessBento (#partner-process) redesigned to mockup layout (mint theme).** Split intro + feature card, 01–07 Discover→Settle step cards (Distribute highlighted), footer CTA → Book a Demo. Live chip rail + prior header/verb list + bento illustration grids preserved as commented code. Append CSS .process-bento-split / -features / -flow / -steps / -cta*. Previous: 2026-08-24 — **Removed `/home-v2` parallel homepage.** Route, `.hv2-*` CSS, and HomeV2 sections deleted; `/` unchanged. Previous: 2026-08-24 — **Homepage remounted to reference structure (our theme).** `/` is Hero → ProcessBento → SellerTools → FeatureOrbit → ToolkitShowcase → Marketplaces → MarketIntelligence → TwoTrack → FinalCTA. Unmounted (files kept): HowItWorks, TravelTools, JourneyNumbers, StickyScrollShowcase, Stats. TravelTools remains on `/products/seatsdeal`. Previous: 2026-08-24 — **Homepage hero copy cut (~20–25%).** Previous: 2026-08-24 — **`/products/seatsdeal` mounts TravelTools.** Previous: 2026-08-24 — **Homepage Phase 2 client cut.** Unmounted (files kept): ProcessBento, ToolkitShowcase, TravelTools, JourneyNumbers. Hero/nav/FinalCTA lead with **Book a Demo**; Become a Seller is secondary. Hero pitch is “Technology powering professional ticket businesses.” SeatsMarket™ FeatureOrbit copy is B2B inventory (“Search. Source. Trade.”). TwoTrack remounted for broker vs B2B. Previous: 2026-08-24 — **Homepage hero typewriter stays one line without overlapping the console.** Green phrases (Ticket Resale / distribute and sell globally. / Scale your ticket business.) are nowrap and shrink to the copy track (`6.8cqi` from 1024px, including the 1024–1439 laptop band). Copy column still `min-width: 0` + `overflow-x: clip`; fluid `minmax(0,1fr)` grid kept. Phone stacked 100svh layout unchanged. Previous: 2026-08-24 — **ProcessBento + Stats headers fill both columns.** Homepage `#partner-process` and `#network-stats` split copy at `≥1024px` (eyebrow + H2 left; body / verbs right) so the right side is no longer blank. Stacked below 1024. Rail, bento cards, and Stats wallet unchanged. HowItWorks / FeatureOrbit / Toolkit / SellerTools / MarketIntelligence / TravelTools / JourneyNumbers / FinalCTA already balanced — left alone. Previous: 2026-08-24 — **Homepage hero headline no longer overlaps the console.** Typewriter wraps inside the copy column on all widths; desktop grid is fluid `minmax(0,1fr)` tracks; laptop type is capped so “distribute and sell globally.” cannot touch the desk. Phone 100svh lock and ≥1024 two-column cinema stay. Previous: 2026-08-24 — **Homepage remounted to the master 11-section brief.** `/` is Hero → ProcessBento → FeatureOrbit → HowItWorks → ToolkitShowcase → SellerTools → MarketIntelligence → TravelTools → JourneyNumbers → Stats (+ FinalCTA in PageShell). This **adds** four sections vs the 7-section Phase 2 cut; it is not a 30–40% cut of this brief. TwoTrack / Marketplaces / StickyScrollShowcase stay unmounted. Hero is Become a Seller + Book a Demo; FinalCTA is Become a Seller + Talk to our team + Login. Previous: 2026-08-24 — **Homepage Phase 2 client cut.** Working tree was restored to `25f6cfd`, then `/` was shortened to Hero → HowItWorks → SellerTools (engine) → FeatureOrbit → MarketIntelligence → TwoTrack → Stats. ProcessBento, ToolkitShowcase, Marketplaces, TravelTools, StickyScrollShowcase, and JourneyNumbers are unmounted (files kept). Hero/nav/footer/FinalCTA lead with Book a Demo; Become a Seller is secondary. SeatsMarket™ copy is B2B inventory (“Search. Source. Trade.”). Console chrome no longer claims 8/16/32 channels or 42ms latency. Previous: 2026-08-24 — **Product Platform integration chain scrolls on narrow screens.** `ProductIntegrationChain` (`.prd-chain-*`) at ≤1023px uses horizontal snap-scroll with hidden scrollbar (matches `.process-bento-rail`) so Intel → Source → Pulse → Market stays on one line — no orphaned `→` at line breaks. Desktop ≥1024px flex-wrap unchanged; current pill mint highlight unchanged. Previous: 2026-08-24 — **Homepage TravelTools (`#travel`) Available Ticket Listings is 8 mock rows** so the vacant table well is filled (4-row cap lifted). Select → Quote → % Margin → Copy / Copy Map / Clear / Download PDF unchanged. Previous: 2026-08-24 — **`/products` Seven products grid is 4+3 on desktop.** `ProductsEcosystem` (`.prd-eco .prd-eco-grid`) at ≥1024px uses flex-wrap + `justify-content: center` with locked **one-quarter** card widths — 01–04 on row 1, 05–07 centered on row 2 (not 3+3+1). Tablet 640–1023 stays 2-col with centered incomplete last row; mobile single column unchanged. `.prd-cap-*` and WorkflowSteps grids untouched. Previous: 2026-08-24 — **Homepage TravelTools (`#travel`) Available Ticket Listings is 8 mock rows** so the vacant table well is filled (4-row cap lifted via `SEAT_MAP_LISTING_ROWS` + extra listings). Select → Quote → % Margin → Copy / Copy Map / Clear / Download PDF unchanged. Previous: 2026-08-24 — **Book a Demo hidden from nav + footer chrome.** Navbar desktop + mobile sheet: Book a Demo `SiteLink` commented out in `Nav.tsx` (Login + Become a Seller remain). Footer Company column: Book a Demo entry commented out in `site.ts` `footerColumns`. `/book-demo` route and page unchanged; heroes/FinalCTA/page CTAs still link there. Previous: 2026-08-24 — **`/products` Seven products grid centers incomplete last rows.** `ProductsEcosystem` (`.prd-eco .prd-eco-grid`) uses flex-wrap + `justify-content: center` at ≥640px with locked 2-col / 3-col card widths — card 07 SeatsFunds™ sits in the middle of the last row (3+3+1 at desktop), not left-aligned. Mobile single column unchanged. `.prd-cap-*` and WorkflowSteps grids untouched. Previous: 2026-08-24 — **Homepage TravelTools (`#travel`) fits 100svh without overlap** on desktop/laptop (`≥900px`). Density trimmed: 4 listing rows, 3 mini rows, filters closed, 1-line titles/notes, side-steps hidden (rail keeps the pipeline). Map chrome still stacks hint → share actions → wrapping legend. Phones/tablets stack and stay non-overlapping; they may exceed 100svh. Laptop `.tpa-fit-zoom` stays `width: 100%`. Previous: 2026-08-24 — **Homepage TravelTools (`#travel`) no longer overlaps.** The 100svh `height`/`max-height` lock was crushing the quote desk: 2×2 mini rows painted on each other, the status strip sat under the cards, and map hint + share actions + nowrap legend shared one band (legend across Download PDF). Section is now `min-height: 100svh` with `height: auto` (no-overlap wins over a hard cap). Mini rows have reserved min-heights + gap. Map chrome stacks map → hint → Copy/Copy Map/Clear/Download PDF → wrapping legend on its own row. Laptop `.tpa-fit-zoom` stays `width: 100%`. Previous: 2026-08-24 — **Homepage TravelTools (`#travel`) fits one desktop viewport.** Section is `height` / `max-height` / `min-height: 100svh` at `≥900px` (overflow still visible — no wheel trap). Available Ticket Listings capped at **6 rows**. Laptop `.tpa-fit-zoom` stays `width: 100%`. Previous: 2026-08-24 — **WorkflowSteps last row is centered on every page that uses it.** Shared `WorkflowInfraCanvas` (`.ws-flow-grid--center`) is flex-wrap + `justify-content: center` with locked card widths: 1-col ≤639, 2-col from 640, 3-col from 768, 5-col from 1024. Seven cards keep 01–05 on the first row; 06–07 sit in the middle (not left under 01–02). Incomplete tablet/mobile rows also center. Card design, copy, order, and green corner marks unchanged. Homepage HowItWorks sticky cinema (`.how-it-*`) untouched. Previous: 2026-08-24 — **Homepage hero Activity-title / overlap-clip pass undone.** DeskFeed is untitled again (no Activity header); the 2.35rem feed clip / 1fr `overflow: hidden` append was removed. Pack-desk table fill, stacked CTAs, and flattened console stay. Previous: 2026-08-24 — **WorkflowSteps last row is centered.** Shared `.ws-flow-grid` (product How, `/products`, `/platform`, and every other WorkflowSteps page) is flex-wrap + `justify-content: center`. Desktop 6+ cards lock to ~20% width so 01–05 stay a five-card row and 06–07 sit in the middle (not left under 01–02). 1–5 step pages still fill the row. Homepage HowItWorks sticky cinema (`.how-it-*`) untouched. Previous: 2026-08-22 — **Homepage hero phone console has no empty well.** Table is a real table again; work pane + rows + feed fill the leftover 100svh card. Desktop ≥1024 unchanged. Previous: 2026-08-22 — **Homepage hero fits iPhone 12 Pro / XR.** CTAs stack ≤639px; console flat, min-width 0, no 3D/scale crop. Desktop ≥1024 unchanged. Previous: 2026-08-22 — **Homepage hero fits small screens.** Phone CTAs `minmax(0,1fr)` (stack ≤429px); console no gutter-bleed, shallower tilt, scale from top so the 100svh lock no longer clips Book a Demo or the desk. Desktop ≥1024 unchanged. Previous: 2026-08-22 — **Product chain current pill is mint, not pink.** `.prd-chain-node[data-current="true"]` fill uses `oklab` + `--primary-soft` (oklch mix with white was a pink wash). Previous: 2026-08-22 — **`/become-a-seller` hero desk fills the clamp.** Apply → Review → Access rows + 5-desk queue + 3 review chips stretch (no empty black well). Form `#apply` compact 100svh unchanged. Previous: 2026-08-22 — **`/become-a-seller` hero matches other product pages.** Dark split `bh-hero` + `bh-layout` (`SellerHero` + `SellerApplyWall`, `.slr-*`): copy 0.92fr, Apply → Review → Access sample queue (not a live login). Application `#apply` is compact on desktop (~`100svh`, 2/3-col fields, `.saf-*` overflow-y safety). Copy in `seller-hero-data.ts` (re-exported from `products-page-data.ts`). Previous: 2026-08-22 — **`/become-a-seller` Key benefits is a 3×2 card grid.** Replaced the disconnected list + infinity orbit with ProductCapabilityBoard language (`prd-cap-*`: icon, index, title, sentence). Copy unchanged. FeatureOrbitGrid unmounted (file kept). Previous: 2026-08-22 — **ProcessBento rail hop is 1:1 with the packet.** Dot used to loop 1.6s while the chip waited 2.1s (two trips then highlight). Packet now runs once per join; highlight advances when it arrives. Settle dwells 2.1s then wraps. Previous: 2026-08-22 — **`/platform` Operating stack console stays sticky.** Desktop right pane was scrolling away because `.plt-flow-grid` used `align-items: start` (column height = console, no sticky travel). Now stretch like HowItWorks (`top: 6rem`, `height: calc(100svh - 6rem)`, steps `46svh`). Homepage section order unchanged. Previous: 2026-08-22 — **Brief claims/CTA/terminology pass (homepage overlap left in place).** FinalCTA is Become a Seller + Book a Demo + Login (`/contact` until app host). ProcessBento audit chips are qualitative (no 8 marketplaces / 182 ms / 2,418). `/platform` and FeatureOrbit Link drop “technology ecosystem” / “API infrastructure.” JourneyNumbers uses Professional Ticket Brokers. Console logs drop latency and marketplace counts. Homepage section order unchanged. Previous: 2026-08-22 — **Homepage ProcessBento rail (`#partner-process`) is a live spine.** Discover → Settle auto-advances every 2100ms while the section is in view (FeatureOrbit dwell: pause on hover, resume 450ms after leave). Active chip is mint-bordered; prior stages read done; the N→N+1 join is the hot hop (brighter dashes + packet). `prefers-reduced-motion` freezes with no interval/packet. Headline, stage names, and bento cards unchanged. Previous: 2026-08-22 — **Source/Pulse/Link/Funds hero consoles fill the stage.** Four new product heroes used the Intel/Market clamp but inner desks were sparse (`space-between` on 3–4 chips, 3-row tables, Pulse rec `margin-top: auto`). Hero visuals only: denser qualitative rows + grid stretch. Unique desks kept. How consoles untouched. Previous: 2026-08-22 — **`/about` is again the short commercial brief (user request).** AboutHero (Built by Ticketing People + AboutJourneyWall kept) → AboutMission (H2 Our Mission; SeatsGroup infrastructure and distribution; five transforms) → AboutClose. Knowledge → Future remount unmounted (files kept). Isolated full-width `.abt-*` tail CSS removed. Previous: 2026-08-22 — **`/about` is a detailed commercial About again.** AboutHero (Built by Ticketing People + AboutJourneyWall kept) → AboutMission (H2 Our Mission; SeatsGroup infrastructure and distribution; five transforms) → Knowledge → Vision → Capabilities → Overview → LiveConsole → Audiences → Principles → Stack → Journey → Building → Future → Close. Full-width `section-curve` + `container-page` like homepage/platform; appended CSS drops 40–52rem max-width on transforms/vision/future/close-pillars and restores full-canvas overview/audiences/verbs. Previous: 2026-08-22 — **Four product heroes match Intel/Market/Deal.** `/products/seatssource`, `/seatspulse`, `/seatslink`, `/seatsfunds` now use unique dark `bh-hero` banners (`SourceHero` / `PulseHero` / `LinkHero` / `FundsHero` + `*ConsoleWall`, prefixes `srh-*` `plh-*` `lkh-*` `fnh-*`) instead of generic `PageHero`. Same split copy + visual, Book a Demo + Become a Seller, `.page-cta-row` 44px. How desks unchanged. Previous: 2026-08-22 — **`/about` is the short commercial brief.** AboutHero (Built by Ticketing People + two brief paragraphs, dark `bh-hero` / AboutJourneyWall kept) → AboutMission (H2 Our Mission; SeatsGroup infrastructure and distribution solutions; “We want to help businesses move from:” + five transforms) → AboutClose (pillars + Become a Seller / Book a Demo / Talk to our team). Unmounted leftover manifesto: Knowledge, Vision, Capabilities, Overview, LiveConsole, Audiences, Principles, Stack, Journey, Building, Future (files kept). Previous: 2026-08-22 — **`/about` leads with the company brief.** AboutHero (Built by Ticketing People + two brief paragraphs, dark `bh-hero` / AboutJourneyWall kept) → AboutMission immediately after (H2 Our Mission; SeatsGroup infrastructure and distribution solutions; transforms kept) → Knowledge → Vision → Capabilities → Overview → LiveConsole → Audiences → Principles → Stack → Journey → Building → Future → Close. No chapters removed. Hero CTAs Become a Seller / Book a Demo / Talk to our team. Previous: 2026-08-22 — **Product pages: brief order + earlier designs.** `ProductStoryPage` now takes `hero` / `how` / `extraCapabilities` slots. Unique heroes and live consoles remounted in How (Intel radar/demand/venue/forecast, Source catalog+inventory, Pulse MI+AI+sample rec, Market channel/listing/sync/price, Deal search/quote/orders, Funds payments+USDT). Capabilities stay `ProductCapabilityBoard` plus EventCategoryBoard / MarketplaceCapabilityBoard. Integration chain + Book a Demo / Become a Seller kept. PartnerTerms still off. Previous: 2026-08-22 — **Individual product pages match the brief skeleton.** All seven `/products/seats*` routes mount `ProductStoryPage`: name + tagline → 2–3 sentence what-it-does → Problem → How SeatsBrokers solves it (`WorkflowSteps`) → Key capabilities (`ProductCapabilityBoard`, 4–6) → Platform integration chain (product pills, e.g. SeatsSource™ → SeatsPulse™ → SeatsMarket™ → SeatsDeal™) → Book a Demo + Become a Seller. Dark two-column hero with the related mini console. `PageShell showFinalCta={false}` so the close is the brief CTA pair, not Talk to the team. Extra event-intel / marketplace / travel / brokers consoles unmounted (files kept). SeatsMarket™ stays generic channels only. CSS `.prd-story-*` / `.prd-chain-*` / `.prd-close-*` append-only. Previous: 2026-08-22 — Product Capabilities card grid on all seven pages. Previous: 2026-08-21 — Homepage TravelTools (`#travel`) matches sibling section width: laptop `.tpa-fit-zoom` no longer shrinks used width to 85% (`display: contents`, `width: 100%`, no `1/0.85` inflate). Gutter is `section-curve-sticky` `margin-inline: clamp(0.25rem, 0.65vw, 0.5rem)` + `container-page` (`max-width: 94rem`, `padding-inline: clamp(0.875rem, 1.5vw, 1.75rem)`) like HowItWorks / SellerTools / Toolkit. `.sto-*` untouched. Previous: `/brokers` Event Catalog Event browser shows **5 reserved rows** (`EventCatalogConsole.tsx` `events.slice(0, 5)` / `browserEvents`; `.ec-catalog-queue` `--ec-queue-row`). Page scroll stays unlocked (`height: auto`, no inner `overflow-y: auto` trap). Copy/£ unchanged. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` untouched. Previous: `/brokers` Live Console desk fills the stage (`.lc-desk-fill`: tilt wrap `width/max-width: 100%` at 375 / 768 / 1024 / 1440 / 1512; was `46rem` / `40rem` leaving a well in the 1.28fr column). AI Pricing empty `.ap-hero` sibling removed; rec + guards + confidence gauge fill the 2-col (not a white 1fr hole). Event Catalog is document-scroll: `.ec-section` / console `height: auto`, `overflow: visible`; dropped locked `.lc-screen` clamp, inner `overflow-y: auto` + `overscroll-behavior: contain`, and `overflow-x: clip` on `.ec-console-wrap`. Same class as Toolkitshowcase’s old `100svh` lock. No 340vh pin. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` / FeatureOrbit / Nav / JourneyNumbers untouched. Previous: Homepage JourneyNumbers (`#journey-numbers`) padding bumped slightly after an over-tight cut: desktop `padding-block: 3.25rem` (cut was `2.5rem`; original was a `100dvh` well). Mobile stack `2.75rem 3rem` (cut `2rem 2.25rem`; original `3rem 3.5rem`). Still content-height, no `100svh` / 340vh pin, no wheel trap. FeatureOrbit / `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` / Nav untouched. Previous: Homepage FeatureOrbit (`#platform`) auto-advances the five product cards every **2100ms** while the section is in view. Solid spoke + detail panel follow `active`. Hover/press on the orbit or a card pauses; resume 450ms after leave. `prefers-reduced-motion` freezes (no interval). Tight bottom pad and no 7.5rem mint well / `lg:py-28` / 340vh pin kept. Previous: Nav lockup is `src/assets/SeatsBrokers-favicon.svg` (full vector lockup: chevrons + wordmark + “Powering Global Ticket Sales”). viewBox `38 49 491 76` trims empty canvas only (ink `40.9–526.3 × 51.4–122.8`, ~2u pad — nothing clipped). Same CSS size at 375 / 768 / 1024 / 1512: **2.5rem** tall, width `calc(2.5rem * 491 / 76)` (~16.15rem / ~258px at 16px root). Only ≤359px (320px bar) shrinks to **2.25rem**. No JPEG/PNG, no `translate` crop. Vector stays sharp at 2x/3x DPR. Footer `logo-sb.png` unchanged. ToolsGrid / toolkit / FeatureOrbit / BrandLockup / `__root.tsx` tab icon (`seatsbrokers-mark.svg`) untouched. Previous: Nav lockup was `src/assets/seatsbrokers-lockup.svg` (viewBox `38 48 492 78`, **2.375rem**). Previous: Nav lockup reverted to PNG `src/assets/logo-2.png` (300×92). JPEG `logo-3.jpeg` crop undone. Ink crop 257×38, `translate(-7.333%, -29.348%)`. Fit height stays **2 / 2.125 / 2.25 / 2.375rem** (~12% zoom-in) in the 4.5rem bar. Footer `logo-sb.png` unchanged. ToolsGrid / toolkit / FeatureOrbit / favicon / BrandLockup untouched. Previous: Nav PNG lockup (`logo-2.png`, `.site-nav-logo-fit`) scaled ~12% so more ink fills the 4.5rem bar: fit height **2 / 2.125 / 2.25 / 2.375rem** (was 1.8 / 1.9 / 2 / 2.125). Ink crop unchanged (257×38, `translate(-7.333%, -29.348%)`). Desktop 2.375rem = 38px matches native ink height. Footer `logo-sb.png` unchanged (does not share the crop class). ToolsGrid / toolkit / FeatureOrbit / favicon untouched. Previous: Reverted BrandLockup (chevron mark + SEATS/BROKERS type). Nav/Footer are PNG `<img>` logos again (`logo-2.png` / `logo-sb.png`). `BrandLockup.tsx` and `.brand-lockup*` CSS removed. `__root.tsx` favicon still mark-only `/seatsbrokers-mark.svg`. Previous: Nav + Footer chrome lockup restored to PNG `src/assets/seatsbrokers-logo.png` (SVG lockup pass reverted; PNG crop `.site-nav-logo-fit` / `.site-nav-logo-img` unchanged). `__root.tsx` favicon still mark-only `/seatsbrokers-mark.svg`. Homepage SellerTools (`#sellers`): 01–06 cards sit **under the engine, between the side rails** (center column row 2; rails span). **5+1** from 1280px; **4+2** at 1024–1279. The red-marked well was a full-width process row after a tall 3-col (`align-items: start` still sized row 1 to the taller rails). Now `grid-template-rows: auto 1fr`, diagram `row-gap: 0.55rem`, board gap `0`. Wrap hops last-of-row-1 → first-of-row-2. Footer stats + italic close unchanged. `.tpa-*` / `.toolkit-*` / `.mps-*` / FeatureOrbit untouched. Homepage FeatureOrbit (`#platform`): bottom pad under the pagination dots cut from equal `padding-block: 4rem` / `5rem` (≥640px) to `4rem 2.25rem` / `5rem 2.5rem` (top unchanged). Orbit-to-dots gap stays `1.25rem`; dots `margin-bottom: 0`. No 7.5rem reserve / 100svh / 340vh pin. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` untouched. Homepage SellerTools (`#sellers`): engine/console stays above; the six sellerSteps cards (Inventory ingest → Settle) sit in the **3-col well between Broker systems and Global buyers** as a **3×2** cluster (not a full-width 01–06 row under the board). Footer stats + italic close unchanged. `.toolkit-*` / `.tpa-*` / `.mps-*` untouched. Homepage Platform toolkit Chrome crash (`Aw, Snap` / STATUS_BREAKPOINT): `.tks-desk` was a `container-type: size` box at `height: 100%` of `.toolkit-screen`, with `@container tks` height queries that hide/show stats/context. That fought `aspect-ratio: 4/3` + desktop `min-height: clamp(..., 32svh, ...)` + `height: auto` after the svh unlock. Size container moved to the stable `.toolkit-screen`; screen height is aspect-ratio only (`min-height: 0`); `.toolkit` overflow is `hidden` on both axes (not x-clip + y-visible). Page scroll kept (no 100svh / 340vh pin). `.sto-*` / `.tpa-*` / `.mps-*` / FeatureOrbit / GlobeCanvas untouched. Homepage FeatureOrbit (`#platform`) is content-height: dropped the desktop 7.5rem `.feature-orbit-detail-reserve` well and `lg:py-28` under the dots. Lower-half nodes open detail upward (stays inside the orbit square). Section is `height: auto` / `overflow: visible` / `padding-block: 4rem 2.25rem` (`5rem 2.5rem` ≥640px). Desktop rings + mobile list unchanged. No 100svh / 340vh pin. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` untouched. Homepage Marketplaces hub lockup is geometrically centered in the disc: crop still **82%** of `--mps-hub-slot` (5.5rem / 6.25rem), but `.mps-hub-logo-fit` no longer uses `translateX(5%)` (that nudge left ~13px / 5px side gaps on the 100px disc). Ink window unchanged (`translate(-7.333%, -29.348%)` on `.mps-hub-logo-img`). 3-col grid and rim joins unchanged. `.sto-*` / `.tpa-*` / `.toolkit-*` / FeatureOrbit untouched. Homepage Marketplaces hub disc is smaller: `--mps-hub-slot` **5.5rem / 6.25rem** (88/100px; was 7rem / 8rem, ~22%). 3-col grid still `minmax(0,1fr) var(--mps-hub-slot) minmax(0,1fr)`. Joins dock across the 0.35rem column-gap (`right/left: -0.35rem`, rail `overflow: visible`). Marquee / `.sto-*` / `.tpa-*` / `.toolkit-*` untouched. Homepage Platform toolkit (`#platform-toolkit`) is document-scroll (not a pinned viewport): `.toolkit` is `height: auto` / `overflow: visible` (dropped `100svh` + `overflow: hidden`); left rail is `overflow: visible` (dropped `overflow-y: auto` + `overscroll-behavior: contain`); console stays `overflow: hidden` for bezel clip only; screen keeps 4/3 + `min-height: clamp(12.5rem, 32svh, 22rem)`. No 340vh pin. `.sto-*` / `.tpa-*` untouched. Homepage SellerTools Connected marketplaces card: **five stacked list rows** (not a horizontal strip) — SeatPick, Hello Tickets, Stubhub, 1BoxOffice, Seatpin as icon-row cards matching Broker systems (primary wash, Live stamp; no ™ on the row; fill copy uses Seatpin™). Generic rails removed. Leftover height below the five rows is typography (16 connected marketplaces + listings-out sentence). One outbound hop per stacked row docks to the card bezel. 3-col diagram, gutters, process row, and overall card size unchanged. `.tpa-*` / Toolkitshowcase / favicon / `__root.tsx` untouched. Document icon mark SVG repaired: `public/seatsbrokers-mark.svg` is now well-formed XML (the previous 7-line file had an illegal U+0013 control character in a comment, which made Cursor’s image preview fail). Chevrons unchanged (`#696D76` / `#64EAA5` from the official lockup). `__root.tsx` still points at `/seatsbrokers-mark.svg` for favicon + apple-touch-icon. Full wordmark lockup is not used as the favicon. Homepage Platform toolkit (`Toolkitshowcase.tsx`) SeatsSource™ / SeatsMarket™ / SeatsFunds™ are static light `ConsoleShell` desks (Ready stamp, `.tks-*`; no timers/packets/scan loops) filling `.toolkit-screen` (`aspect-ratio: 4/3` kept, desktop `min-height: clamp(12.5rem, 32svh, 22rem)` + flex-fill so the pane cannot collapse). SeatsIntel™ / SeatsPulse™ stay product images. Homepage SellerTools named marketplace tiles (SeatPick / Hello Tickets / Stubhub / 1BoxOffice) are highlighted live (primary wash, Live stamp, in-view pulse); generic rails Direct API / Partner feed / File drop / Webhook out stay muted with subtitles; copy says 16 connected marketplaces; process cards are one centered row of 01–06 from 1024px with five gutter hops (3×2 wrap only at 900–1023). Homepage TravelTools 2×2 minis are equal-height (same header / 2-line note / dial / 4-row board / footer slots); leftover Live-column space is filled by pipeline chrome above (Pipeline 01–04, Select → Quote → Margin → Share) and a status strip below, not `place-content: center`. Homepage TravelTools (`#travel`) page scroll: nested `overflow: auto` on `.lc-body` / `.smt-table-wrap` inside the old `100svh` + `overflow: hidden` lock no longer traps wheel (same as HowItWorks). Homepage SellerTools (`#sellers`, `.sto-*`) is a marketplace connectivity & orchestration infographic (systems → engine → five named live marketplaces stacked as list rows + typography fill + category buyers; no extra competitor names). `/brokers` BrokerOnboardingSection **layout/UX**: one-open six-phase accordion (inactive = `01 Apply` row), dense auto-height ConsoleShell rail + 3-line ledger, 4-col comparison scrolls in-card, tighter partners/FAQ; `[CONFIRM]` copy unchanged. Sticky console only ≥1024px and ≥1100px height. Homepage TravelTools (`#travel`) laptop zoom no longer inflates `.tpa-fit-zoom` by 1/0.85 (that overflow-clipped the right minis); `zoom: 0.85` with `width: 100%` so 2×2 cards stay fully inside the section. Mini titles still wrap. `/brokers` BrokerOnboardingSection **full rebuild** (six parts, not a copy edit): header + Dubai · London · India · New York region tabs; regional comparison table; six-phase journey (We handle / You provide / region detail) with compact `ConsoleShell` auto-run (`seatsbrokers / brokers / onboarding`, reserved rail + `key.event → outcome` ledger); partner sub-accounts; region-aware checklist; FAQ accordion. Region tab drives parts 3–5. No fabricated weeks/hours/fees — `[CONFIRM: …]` comments in `broker-onboarding-data.ts`. Rail + ledger stay separate reserved rows (no bezel spill). Not a LiveConsole variant; SeatsFunds™ USDT console is not duplicated. Homepage Testimonials top cap is a 1.35rem diagonal band (`#595C62` left / `#4BE5AE` right, clip-path 58%→38% slash); pale mint body and teal stars unchanged. Stats wallet cards stay the pre-slab light `.stats-wallet-*` look. Shuffle still replays on enter and resets when out of view. Homepage `GlobeScrollSection` de-duplicated to 2 unique cards (New York ecosystem + Dubai B2B). London, Singapore, and Sydney dropped as restatements of card 1. Scroll index, pin dots, and last-card pad follow `slides.length`; GlobeCanvas hubs stay decorative. `/brokers` SeatsFunds™ payout track: both USDT and Standard cards fully in view (no 7.5rem min-height clip); track panel grows to content (`overflow: visible`, no `height: 100%`). Custom scrollbars are one global recipe (`--primary` thumb, `--primary-deep` hover, light `--surface`/`--background` track on `html`/`body`/`*`; dark `--dark` mix track only inside `.bh-hero` / `.bg-dark` / `.toolkit`). `/brokers` SeatsFunds™ USDT Live Console redesigned: wallet / on-chain transfer desk (sale → commission withheld → USDT transfer → settled), qualitative Standard vs crypto switch, no volume ladder or sale £/USDT amounts. User-visible UI is sentence case (first letter capital; brands/acronyms kept): 242 CSS `text-transform: uppercase` rules set to `none`, Tailwind `uppercase` stripped from landing/chrome, console stamps `LIVE`/`QUOTE`/`SYNC` → Live/Quote/Sync. Nav product names stay title case. Homepage Hero pill is smaller (0.6875rem / 0.18rem 0.5rem, no `.section-eyebrow` uppercase) with more top pad (`.hero-fit-offset` 4.5rem+1.75rem mobile; desktop clamp 7.5–9.25rem; laptop 1024–1919 bumped to 8.65–11rem so 0.85 zoom still reads extra). Homepage TravelTools `% Margin` popover is fully visible: anchored to `.smt-list-head` (`right: 0`, `width: min(17rem, 100%)`) so the 100svh overflow lock no longer crops “Set Margin Percentage”; desk gaps unified (`--tpa-gap` / `--tpa-gap-inner`). Homepage TravelTools (`#travel`) overflow panes inherit the global `--primary` scrollbar (no scoped `.tpa-section` colors). `/brokers` SeatsFunds™ payout desk (`LiveConsole variant="cryptoPayouts"`, `CryptoPayoutConsole`, `.sfp-*`): 10%→7% commission on £ volume bands, Standard (0% payout fee) vs USDT (+5% crypto fee), live ladder + reserved-height maths; sits after Payment Infrastructure. Homepage TravelTools (`#travel`) is a full-viewport quote desk: reserved Auto-run rail, then Live console + 2×2 minis inside `100svh` (`.tpa-section`; stacks below 900px). Homepage venue map: centered zoom/reset/close icons, in-block section/block labels, selected-count pill in the map header (toast no longer overlaps Hide Filters). (Nav logo mobile restore: `.site-nav-logo-fit` now uses a definite rem width+height and `flex-shrink: 0` — 1.8rem / ~12.2rem at default, 1.9rem from 400px, 2.0rem tablet, 2.125rem desktop; dropped circular `max-width: 100%` / `min-width: 0` that collapsed the crop window. Bar still 4.5rem.) Marketplaces hub current `.mps-hub*` rules including `.mps-hub-logo-fit` restored as an appended `styles.css` block after a rewrite dropped them — 3-col `minmax(0,1fr) var(--mps-hub-slot) minmax(0,1fr)`, 82% crop, 5% optical nudge, **88/100px** circle (was 112/128px), chip min-width 9.25rem. White circle is a fixed-width center column so marquee width cannot shift it; lockup crop **68% → 84% → 82%** with `translateX(5%)`. User-visible “travel” copy → **B2B** — Travel Partners/companies/agencies/operations → B2B partners/companies/agencies/operations; `/travel-partners` route, files, CSS classes and component names unchanged. Homepage TravelTools is a live Seat Map & Tickets quote desk: select → Quote → % Margin apply → Copy / Copy Map / Download PDF, with three synced pipeline cards. SellerTools diagram, Nav, Hero, and About unchanged. Nav logo Home is `to="/"` with `scrollTo(0)` — no `/#top` hash. Homepage hero: copy column and 3D console no longer overlap — desktop `lg:pr` / `lg:pl` gutters, copy `z-20`, tilt origin `right center`, left-bleed clip; typewriter nowrap caret unchanged.) Homepage Testimonials top cap is a 1.35rem diagonal band (`#595C62` left / `#4BE5AE` right, clip-path 58%→38% slash); pale mint body and teal stars unchanged. Stats wallet cards stay the pre-slab light `.stats-wallet-*` look. Shuffle still replays on enter and resets when out of view. Homepage `GlobeScrollSection` de-duplicated to 2 unique cards (New York ecosystem + Dubai B2B). London, Singapore, and Sydney dropped as restatements of card 1. Scroll index, pin dots, and last-card pad follow `slides.length`; GlobeCanvas hubs stay decorative. `/brokers` SeatsFunds™ payout track: both USDT and Standard cards fully in view (no 7.5rem min-height clip); track panel grows to content (`overflow: visible`, no `height: 100%`). Custom scrollbars are one global recipe (`--primary` thumb, `--primary-deep` hover, light `--surface`/`--background` track on `html`/`body`/`*`; dark `--dark` mix track only inside `.bh-hero` / `.bg-dark` / `.toolkit`). `/brokers` SeatsFunds™ USDT Live Console redesigned: wallet / on-chain transfer desk (sale → commission withheld → USDT transfer → settled), qualitative Standard vs crypto switch, no volume ladder or sale £/USDT amounts. User-visible UI is sentence case (first letter capital; brands/acronyms kept): 242 CSS `text-transform: uppercase` rules set to `none`, Tailwind `uppercase` stripped from landing/chrome, console stamps `LIVE`/`QUOTE`/`SYNC` → Live/Quote/Sync. Nav product names stay title case. Homepage Hero pill is smaller (0.6875rem / 0.18rem 0.5rem, no `.section-eyebrow` uppercase) with more top pad (`.hero-fit-offset` 4.5rem+1.75rem mobile; desktop clamp 7.5–9.25rem; laptop 1024–1919 bumped to 8.65–11rem so 0.85 zoom still reads extra). Homepage TravelTools `% Margin` popover is fully visible: anchored to `.smt-list-head` (`right: 0`, `width: min(17rem, 100%)`) so the 100svh overflow lock no longer crops “Set Margin Percentage”; desk gaps unified (`--tpa-gap` / `--tpa-gap-inner`). Homepage TravelTools (`#travel`) overflow panes inherit the global `--primary` scrollbar (no scoped `.tpa-section` colors). `/brokers` SeatsFunds™ payout desk (`LiveConsole variant="cryptoPayouts"`, `CryptoPayoutConsole`, `.sfp-*`): 10%→7% commission on £ volume bands, Standard (0% payout fee) vs USDT (+5% crypto fee), live ladder + reserved-height maths; sits after Payment Infrastructure. Homepage TravelTools (`#travel`) is a full-viewport quote desk: reserved Auto-run rail, then Live console + 2×2 minis inside `100svh` (`.tpa-section`; stacks below 900px). Homepage venue map: centered zoom/reset/close icons, in-block section/block labels, selected-count pill in the map header (toast no longer overlaps Hide Filters). (Nav logo mobile restore: `.site-nav-logo-fit` now uses a definite rem width+height and `flex-shrink: 0` — 1.8rem / ~12.2rem at default, 1.9rem from 400px, 2.0rem tablet, 2.125rem desktop; dropped circular `max-width: 100%` / `min-width: 0` that collapsed the crop window. Bar still 4.5rem.) Marketplaces hub current `.mps-hub*` rules including `.mps-hub-logo-fit` restored as an appended `styles.css` block after a rewrite dropped them — 3-col `minmax(0,1fr) var(--mps-hub-slot) minmax(0,1fr)`, 82% crop, 5% optical nudge, **88/100px** circle (was 112/128px), chip min-width 9.25rem. White circle is a fixed-width center column so marquee width cannot shift it; lockup crop **68% → 84% → 82%** with `translateX(5%)`. User-visible “travel” copy → **B2B** — Travel Partners/companies/agencies/operations → B2B partners/companies/agencies/operations; `/travel-partners` route, files, CSS classes and component names unchanged. Homepage TravelTools is a live Seat Map & Tickets quote desk: select → Quote → % Margin apply → Copy / Copy Map / Download PDF, with three synced pipeline cards. SellerTools diagram, Nav, Hero, and About unchanged. Nav logo Home is `to="/"` with `scrollTo(0)` — no `/#top` hash. Homepage hero: copy column and 3D console no longer overlap — desktop `lg:pr` / `lg:pl` gutters, copy `z-20`, tilt origin `right center`, left-bleed clip; typewriter nowrap caret unchanged.)

## Current state

**Book a Demo modal:** `DemoModalProvider` wraps PageShell. FinalCTA / Hero open a **single-column stack** dialog (`data-layout="stack"`); nav Book a Demo is hidden (commented in `Nav.tsx`): top bar + mint SeatsBrokers title + 4-col USP + icon form + 2-col `moduleList`/API product cards (green border + check) + Tell us more + Prefer `/book-demo` + mint “Send message →” + lock privacy. Panel `max-height` ≤100svh; only body thin-scrolls. Full `/book-demo` page kept. Other page heroes still `SiteLink` to `/book-demo`.

**Homepage TwoTrack (`#platform-tracks`):** Two Journeys — both Demo desks share fixed `--tt-desk-h: clamp(280px, 52svh, 420px)` (short laptops `clamp(240px, 48svh, 340px)`). Intro is a ProcessBento/Stats-style L/R grid at ≥1024. Identity cards are horizontal (icon + role label + headline + body + pill CTA; mint / slate). **Equal height ≥1024:** stage `align-items: stretch`, columns fill, id cards `flex: 1`, CTA pinned with `margin-top: auto`. Section `min-height: 100svh` (may grow). Consoles untouched. Broker → Explore products; B2B → Explore SeatsDeal™. Page scroll unlocked. Mobile stacks, auto height.

**Homepage ProcessBento (#partner-process):** three-band reference (mint theme) — left **The ticketing challenge** + H2 (mint second clause) + pill callout; right **systems diagram** (Your systems → SeatsBrokers engine → Your channels) with in-view arrow hop; centered **One connected workflow.** + **live** 01–07 Discover→Settle vertical cards (`ProcessFlow`: in-view packet hop ~2100ms, active mint border + wash, done priors, click jump, hover pause/450ms resume, reduced-motion freeze, mobile scroll-into-view); 4-up proof bar; footer Book a Demo CTA (`openDemoModal` + global button). Prior feature card, live chip rail, verb header, and illustration bento grids kept as commented code. Append `.process-bento-diagram*` / `-proof*` + callout-pill overrides.

**Individual product pages (`ProductStoryPage`):** brief **order** with unique dark heroes on all seven. Intel `EventIntelHero`, Market `MarketplaceHero`, Deal `TravelPartnersHero`, Source `SourceHero` (catalog + 6-event ingest + 5-row inventory sheet), Pulse `PulseHero` (6 signals + 3-col rec £247/£285/£265 + notes, no fake %), Link `LinkHero` (6-row POS queue + 5-stage pipe), Funds `FundsHero` (settlement rails + 5-row queue / Standard·USDT qualitative). Hero rooms fill the same clamp as Intel/Market (header + body + footer stretch; no empty lower band). Tagline + what-it-does fold into the hero → Problem → How steps → remounted live consoles → `ProductCapabilityBoard` (+ EventCategoryBoard / MarketplaceCapabilityBoard) → product chain → Book a Demo + Become a Seller. Global FinalCTA off.

**Product Capabilities (`ProductCapabilityBoard`, `.prd-cap-*`):** all seven `/products/seats*` pages use a light 3×2 hairline-card grid (1-col / 2-col ≥640 / 3-col ≥1024) with a 44px icon well, index, title and one sentence — the same language as `/api` `ApiInfraBoard`. Copy stays in `productStories.*.capabilities`. SeatsMarket™ now has the section (List once, Marketplaces, B2B Buyers, Websites, API Partners, Sync on sale — generic channels only). Cards-first; no extra ConsoleShell; no FeatureOrbitGrid. FeatureGrid remains on `/become-a-seller` only.

**Document icon (`src/routes/__root.tsx`):** favicon and apple-touch-icon use `/seatsbrokers-mark.svg` — the two official chevrons only (`#696D76` / `#64EAA5`), viewBox-cropped from `public/SeatsBrokers-01 (1).svg`. Wordmark omitted so the tab icon stays readable at 16–32px. Nav uses SVG `src/assets/SeatsBrokers-favicon.svg` (chevrons + wordmark + tagline, viewBox `38 49 491 76`); Footer uses PNG `src/assets/logo-sb.png`. Console URL bars untouched.

**Homepage FeatureOrbit (`FeatureOrbit.tsx` `#platform`):** auto-advances the five product cards (`0→4`) every **2100ms** while the section is in view (`useInView` `once: false`, threshold 0.16). Solid spoke + detail popover follow `active`. Hover or press on the orbit canvas / a card pauses; resume 450ms after leave. `prefers-reduced-motion` freezes on the current card with no interval. Click still selects a card and restarts the dwell. Tight bottom pad (`4rem 2.25rem` / `5rem 2.5rem`), upward detail for lower-half nodes, no 7.5rem mint well, no `lg:py-28`, no 100svh / 340vh pin, no wheel trap. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` / Nav untouched.

**Homepage Platform toolkit (`Toolkitshowcase.tsx` `#platform-toolkit`, `.toolkit-*` / `.tks-*`):** five-module rail still auto-advances. SeatsSource™ / SeatsMarket™ / SeatsFunds™ are **static** light `ConsoleShell` desks (Ready stamp; no `setInterval`, packets, or `.lc-scan` loops) that fill `.toolkit-screen`. Source: ingest table (section/qty/£/sync ticks) + compact map/holds. Market: SeatPick / Hello Tickets / Stubhub / 1BoxOffice / Direct API / Partner feed with mirrored £ ask. Funds: partner settle rows in £ + Standard / USDT rails (USDT qualitative, no crypto amounts). SeatsIntel™ and SeatsPulse™ stay product screenshots. The section is normal document flow: `.toolkit` is `height: auto` / `overflow: hidden` on both axes (no `100svh` lock; never x-clip + y-visible). Wheel/trackpad over the left rail, console, or empty space scrolls the page — rail is `overflow: visible` (no `overflow-y: auto` / `overscroll-behavior: contain`); `.toolkit-panel` / `.toolkit-main` / `.toolkit-screen` clip the bezel. Screen height is **aspect-ratio 4/3 only** (16/11 below 640px) — no desktop `min-height: clamp(..., 32svh, ...)` and no `height: 100%` of an auto parent. `container-type: size` / `container-name: tks` is on that stable screen, not on `.tks-desk` (that pairing + height-based `@container` hide/show was the Chrome STATUS_BREAKPOINT loop). Desks still fill the pane (not height 0). Desktop stays two-column; the stage scrolls away with the page (no sticky/fixed, no 340vh pin). `.sto-*` / `.tpa-*` / GlobeCanvas / FeatureOrbit untouched.

**Homepage SellerTools (`ToolsGrid.tsx` `#sellers`, `.sto-*`):** marketplace connectivity infographic. Header is HowItWorks-style text (no logo). Light `ConsoleShell` engine (connect layer + modules + unified data) stays in the diagram center. Desktop diagram is **2-row with spanning rails**: Broker systems and stacked right (Connected marketplaces on top, Global buyers below) **span both rows**; engine is center row 1; **six sellerSteps** sit in **center column row 2** — the well under the engine between the side cards. Cluster is **5+1** from `≥1280px` (`repeat(5, minmax(0, 1fr))`) and **4+2** at `1024–1279` (`repeat(4, minmax(0, 1fr))`). `grid-template-rows: auto 1fr` + `row-gap: 0.55rem` so the cards tuck under the console instead of waiting below the taller rails. `.sto-board` gap `0`. Compact step type. `StoProcessFlow` hops along row 1 then wraps to the next row. `900–1023` is 5+1 stacked under the diagram; below 900 CSS arrows / stack. Side-rail hops stay on the engine gutters (sys rows no longer 1fr-stretch into the well). **Packets** travel while the board is in view (`data-live`); a follow packet at half-duration keeps the line alive across loop wrap. No hover-hold. `prefers-reduced-motion` freezes. Active card follows the 01–06 engine cycle. Marketplace rail: **five stacked list rows** (icon-row cards like Broker systems, not a 1×5 strip) — SeatPick, Hello Tickets, Stubhub, 1BoxOffice, Seatpin (`--primary` border/inset, 10% wash, Live stamp, in-view pulse; title only, no ™ on the row). Generic rails removed. Leftover vertical space below the five rows is a type well: **16 connected marketplaces** plus a sentence-case Seatpin™ listings-out line. Footer stats + italic close stay below the board. TravelTools / `.tpa-*` / `.toolkit-*` untouched.

**Homepage Stats (`Stats.tsx` → `StatsAccentLight`, `.stats-wallet-*`):** four proof cards (30+ years, 10K+ B2B partners, 32 marketplaces, 165 countries) start as a fanned wallet deck. `useInView({ once: false, rootMargin: "-10% 0px -12% 0px" })` plays the peel/riffle/spread on every enter (2×2 below 1024px / 4-col desktop) and resets to the stacked wallet when the section leaves view (timeouts cancelled, `animation: none` on wallet so no mid-deal leftover). Grid cells hold the settled height during the shuffle. `prefers-reduced-motion` shows the spread immediately and stays settled. Ledger and glow variants remain unmounted.

**Homepage TravelTools (`ToolsGrid.tsx` `#travel`, `.tpa-section` / `smt-*`):** SeatsDeal™ intro is unchanged. Section width matches HowItWorks / SellerTools / Toolkit: `section-curve-sticky` + `container-page` (`94rem` / `clamp(0.875rem, 1.5vw, 1.75rem)`). Laptop `.tpa-fit-zoom` stays `width: 100%` without `1/0.85` compensation. Desktop/laptop (`≥900px`) is a quote-desk viewport locked to `height` / `max-height` / `min-height: 100svh` **without overlap**: Available Ticket Listings is **8 rows** (`SEAT_MAP_LISTING_ROWS`); each 2×2 mini shows **3 reserved one-line rows**; filters start closed; titles/notes clamp to one line; side-steps hide (the Auto-run rail still shows Select → Quote → Margin → Share). Left Live `ConsoleShell` Seat Map & Tickets desk; right `.tpa-side` caption + equal-height minis + status strip below the cards. Map chrome stacks map → hint → share actions (44px Copy / Copy Map / Clear / Download PDF) → wrapping category legend (`.smt-desk-chrome`). Wheel anywhere continues document scroll — no nested overflow-y-auto; table/map are overflow-x only. Below 900px it stacks (rail → live → caption → 2×2 → status, 1×4 under 480px), stays non-overlapping, and may exceed 100svh on phones. Desk (`SeatMapTicketsConsole.tsx`, `seat-map-tickets-data.ts`) is Manchester City vs Liverpool at the Etihad (£). Shared React state: checkbox select → Quote → % Margin → Copy / Copy Map / Clear / Download PDF. SellerTools (`#sellers`, `.sto-*`) is unchanged.

**`/legal` is one legal page (not three sites, not a product console):** full-width dark `bh-hero` copy banner (no 46rem prose column, no right-stage chapter rail; `.legal-shell` uses `padding-inline: clamp(1rem, 3vw, 2rem)`). Light body is three equal tab cards in a row (Privacy | Terms | Cookies) plus one full-width policy panel; desktop policy blocks fill 2–3 columns. Hashes `/legal#privacy`, `/legal#terms`, `/legal#cookies` select the matching tab so footer links land correctly. Copy is limited to project facts (SeatsBrokers, B2B brokers/travel partners, London · New York · Dubai, sales@ and partners@, £). No invented company number. Footer `footerLegal` stays on this page; API Terms stays `/api`. CSS: `.legal-*` only.

**`/faq` is a FAQ page (not a product-console stack):** FaqHero reuses dark `bh-hero` / `bh-stage` chrome (gradient, orb, copy icon, extra top/bottom padding so copy clears the overlay nav including MacBook). Left copy inverts to light type (`bh-title` / `bh-body`) with `--primary` eyebrow and italic “We've got answers.” (Space Grotesk, not a second face). Right stage stays unique: a dark-glass 06 TOPICS rail (green indices; not a brokers/marketplace dashboard). Accordion and help strip unchanged (light list, then dark mailto band). Six questions in `FaqAccordion` (shadcn Accordion, `faq-*`): inventory types, API for brokers and travel partners, authenticity, travel-agency delivery, bulk/holds, platform charges in £. `FaqHelpStrip` is a dark still-need-help band with mailto cards to `sales@seatsbrokers.com` (brokers, marketplaces & API) and `partners@seatsbrokers.com` (travel partners) — replies within one business day; no Book a Demo. Copy lives in `faq-data.ts`. FinalCTA via PageShell only (Become a seller → `/brokers`). Footer Resources “FAQs” now points to `/faq` (was `/contact`); header nav does not list FAQ. **Phone / small-tablet (`≤639px`, 320–430):** topic index stacks 1-col under 430px and 2-col to 639px; accordion triggers `min-height` 44px with wrapping questions; emails `overflow-wrap: anywhere`; no horizontal overflow. Tablet (`640–1023`): 3-col topic chips, two help cards. Desktop (`≥1024px`): two-column hero (copy + vertical topic ledger) and two-column help strip. Not a clone of brokers/PageHero.

**Homepage who-we-serve (`StickyScrollShowcase`, `sss-*`):** section titles/H2s are sentence case (no Tailwind `uppercase`); kickers stay `section-eyebrow`. The sticky screenshot column is now four unique light `ConsoleShell` mini-consoles (`StickyScrollConsoles.tsx`) bound 1:1 to the audience steps — broker distribution (channel pips + listing table + ask spark), travel quote desk (£ cost / margin / client + PDF/WhatsApp/email), marketplace sync (ingest→order rail, ask vs floor, channel lag), partner API bridge (endpoint cycle + signed JSON). All four share a fixed `--sss-console-h` on `.sss-stage` (phone `clamp(20.5rem, 56svh, 24.5rem)`, default/mobile `clamp(22.5rem, 58svh, 28rem)`, tablet `clamp(26rem, 48vh, 30rem)`, desktop `clamp(28rem, 58dvh, 34rem)`) so sticky swaps do not jump; shorter broker content flex-fills `.lc-body` / `.sss-work`. Desktop (`≥1024px`) keeps copy scrolling and the right console sticky, sliding with the active step; below `lg` each console stacks above its copy (readable at 375px). Live ticks run only on the active/in-view console; `prefers-reduced-motion` freezes them and still shows the chrome. Not a clone of brokers/marketplace/travel heroes.

**Homepage hero backgrounds:** three stadium photos in `src/assets/hero-stadium-{1,2,3}.*` (console 0→1, 1→2, 2→3) crossfade on the same `active` index / `SLIDE_MS` 2100 as the light HUDs; the overlay is a black vignette (center starts at `0.54` black, 38% `0.60`, mid `0.72`, edges `0.90`; plus top/left linear — no `primary-deep` green wash). **Homepage hero console (`HeroDashboardTilt`, `hero-hud-*`):** the tilt stage is light glass (white/off-white panels, gray chrome, dark text, `--primary` accents; still 3D-tilted) and renders one of three unique consoles bound 1:1 to the hero slide (auto-advance `SLIDE_MS` 2100, independent of typewriter) — platform layers + scrolling system feed (slide 1), listing × channel distribution matrix + export-close bar (slide 2), partner margin/quote desk with £ client price (slide 3) — swapping on auto-advance with a quick jump-up slide-in; the typewriter line reserves its longest phrase (ghost line-box) and all three consoles share the same fixed `28rem` frame, so no slide change resizes the hero; mobile `100svh` lock, console `--hero-console-scale` `0.85` (`0.82` under 700px height) filling the leftover flex slot (`flex: 1 1 0`, no `--hero-scene-h` cap); shallow 3D tilt kept; CTAs sit in a 2-column wrap on mobile (not stacked full-width rows); phone copy is pill + headline + typewriter + short body + CTAs (long lead/dense/tags still hidden; type shrinks under 700px instead of `display:none`).

Marketing site is live as a TanStack Start app. Homepage (`/`) is a full landing narrative (Hero through Testimonials) wrapped in PageShell (Nav + FinalCTA + Footer). **Phone/small-tablet homepage** (`≤639px` and below `lg`) uses stacked layouts: homepage hero is locked to `100svh` (nav ~4.5rem spacer; reduced type — pill + headline + typewriter + short body + 2-column CTAs; long lead, dense body/bullets and tags hidden; headline `line-height` 1.25 + padding so iOS does not crop ascenders; tilt console fills the leftover flex slot at `--hero-console-scale` `0.85` / `0.82` under 700px height; next section does not peek); NetworkConstellation is a `100svh` section at `≥1024px` (globe + rail + copy clamped to fit one viewport, including 1280/1440/1512/1680/1920/2560; mobile stays auto-height with a reserved console box so stage swaps do not change section height); chips/rail/nodes/dots still click to a stage and restart a 1.8s auto-advance (keeps running on hover; `prefers-reduced-motion` off) — no 340vh pin or scroll-scrub; title/body/metric sit in ghost-reserved slots like the hero typewriter; Marketplaces is a full-width hub (SeatsBrokers logo in a white squircle; left/right glass pods fill the row and stream items inward — left LTR, right RTL — Events / Inventory / Listings in, Marketplaces / Travel partners / POS out; stacks top→logo→bottom below `lg`, `prefers-reduced-motion` is static); FeatureOrbit is a list; JourneyNumbers is the card stack; TwoTrack cards straighten once on in-view (no GSAP pin/scrub); Toolkit/Globe/StickyScroll/HowItWorks drop sticky/min-viewport cinema and stack. Desktop HowItWorks (`≥1024px`) is a two-column cinema: left step cards scroll, right product screenshot stays sticky and swaps with the active step; wheel over the screenshot scrolls the page (no nested overflow-y-auto). Desktop (`≥1024px`) homepage hero keeps the two-column cinematic layout: copy and console sit as one centered unit (`40rem` + `44rem` columns, `container-page` 94rem, tops aligned); the section is a flex column (`min-height: 100dvh`, height auto — not a shrinking `minmax(0,1fr)` grid) with `.hero-fit-offset` `clamp(6.75rem, 5.25rem + 4vh, 8.5rem)` so content clears the fixed nav; **laptop band 1024–1919px** (`1512×982`, `1440×900`, `1680×1050`) zooms `.hero-fit-zoom` (offset + copy + console + CTAs + dots) to `0.85` so the full hero fits without bottom crop (`@supports not (zoom)` uses `scale(0.85)` from top center); **≥1920px** leaves the hero at 100% (no second 0.85 on top of any large-screen html zoom) and only console-scales under 980/900/820px height; dots `padding-bottom: clamp(1.75rem, 3vh, 2.75rem)`; nav uses the same 94rem max-width as page content. Mobile `100svh` lock, `4.5rem` offset, and `display: contents` on `.hero-fit-zoom` are unchanged. Cinematic layouts otherwise unchanged. Header nav (`src/components/landing/Nav.tsx`, links in `src/content/site.ts`) highlights the current route in brand `--primary` green (underline + color) on desktop and mobile. Visible product links are **Marketplace Connectivity** (`/marketplace-connectivity`), **Event Intelligence** (`/event-intelligence`) and **API** (`/api`); For Brokers and About stay as-is. Footer Platform column uses the same three descriptive labels (module TM names stay on page heroes, not chrome). Official module names and taglines live in `src/content/modules.ts`. **Phone / tablet nav (`<1024px`):** logo left + 44px hamburger right in the same 4.5rem bar (hero offset unchanged); full-bleed sheet below the bar (right sheet + dimmed backdrop from 768px) with scrollable links, pinned Login (no Become a seller in nav), body-scroll lock, and close on route change / Escape / backdrop; sheet focuses the dialog (not For Brokers) and iOS tap/focus rings are off on coarse pointers; Platform and Travel Partners stay hidden; About is visible. The header bar is always opaque `var(--background)` (white; fixed overlay, no glass/blur) with a muted `--border` hairline and dark text (active `--primary`). The header lockup is SVG `src/assets/SeatsBrokers-favicon.svg` on the light bar (viewBox `38 49 491 76`, **2.5rem** tall at 375/768/1024/1512, **2.25rem** only ≤359px); the dark footer uses PNG `src/assets/logo-sb.png` with `.brand-logo-on-dark`; FeatureOrbit (white disc) keeps the original PNG. Matching is exact or nested prefix (`pathname === to || startsWith(\`${to}/\`)`); the logo only matches `/` (`activeOptions.exact`). **Platform and Travel Partners are hidden from the visible nav** (`hidden: true` on those links) — `src/routes/platform.tsx` and `src/routes/travel-partners.tsx` still serve their URLs. About is a visible nav link to `/about`. Footer (`footerColumns`) no longer lists Platform Overview, Integrations, Careers, Market Analytics, or AI Pricing; those routes still exist. Primary chrome CTA is **Become a seller** (`ctas.becomeSeller` → `/brokers`); Book a Demo is commented out in `site.ts` and `/book-demo` is unlinked from chrome (route kept). Footer partners line is `Partners: sales@seatsbrokers.com · partners@seatsbrokers.com` (mailto); social icons are commented out. Product-page heroes (`.bh-hero` and the first `.section-curve` in `<main>`, including `PageHero`) use `margin-top: clamp(0.25rem, 0.65vw, 0.5rem)` to match `.section-curve`’s side gutter so the rounded card is not flush to the fixed nav; the homepage cinematic hero (`section-curve-hero`) stays full-bleed.

**`/brokers` is deep:** BrokersHero wall, Payment Infrastructure dashboard, Broker company setup (`BrokerOnboardingSection`: six parts — region strip Dubai/London/India/New York, 4-col comparison with in-card scroll, one-open six-phase accordion plus compact `ConsoleShell` auto-run, partner sub-accounts, checklist, FAQ; `.bon-*`), SeatsFunds™ USDT/crypto payout Live Console (`cryptoPayouts` / `CryptoPayoutConsole`: USDT wallet rail as hero, vault→on-chain→wallet path, qualitative Standard vs crypto switch, reserved-height ledger/feed — no volume ladder or sale maths), Event Catalog console, Inventory Management console, SyncDiagram, SectionConnectors, Market Intelligence + AI Pricing live consoles, WorkflowSteps (POS → API), ApiCards. Live Console stage (`.lc-desk-fill`) fills the 1.28fr column at 375 / 768 / 1024 / 1440 / 1512 — tilt wrap is `width: 100%` (not `max-width: 46rem`). AI Pricing `.ap-hero` is rec + guards only (empty `app` column gone; confidence gauge restored). Event Catalog (`EventCatalogSection` / `.ec-*`) is content-height document flow: section + `ConsoleShell` `height: auto`, `overflow: visible`; no locked `.lc-screen` clamp, no inner `overflow-y: auto` / `overscroll-behavior: contain`, no `overflow-x: clip` on `.ec-console-wrap`. Event browser is **5 reserved rows** (`EventCatalogConsole.tsx` `events.slice(0, 5)` / `.ec-catalog-queue` `--ec-queue-row`). Wheel over the catalog scrolls the page. No 340vh pin. Market Intelligence (`mi-*`) stacks ASK LADDER and SIGNAL FEED as separate regions: the grid flexes and clips, the feed is reserved below with its own overflow — ladder rows no longer paint through the feed. **Phone / small-tablet (`≤639px`, and overflow-clip through `1023px`):** copy stacks above live-console tilt stages; stages flatten and clip so they fit the viewport; hero wall uses existing `.bh-wall-mask` clamps (`clamp(16rem, 38vh, 22rem)`); Event Catalog `.lc-screen` is auto-height (page scroll); Market Intelligence `.lc-screen` heights stay clamped; WorkflowSteps cards stay single-column. Desktop (`≥1024px`) Live Console desk is full stage width. No horizontal page overflow at ~375px.

**`/travel-partners` is deep:** TravelPartnersHero (left copy unchanged; right stage is a dark partner-desk analytics dashboard — KPIs, £ earnings float, weekly bookings + inventory fill, progress lists, order-status float; primary green, `tpd-*`), Inventory Access console (markets + partner view + margin preview), Inventory Access stacked-card showcase (catalog + detail overlay), Search filter zoom (diagonal glass cards: unfiltered list → filtered event; console-density tables inside both cards), Margin Management (£500 / 10% / £550), Quotation Builder console, Order & Delivery FeatureGrid + fulfillment desk (`PartnerOrdersConsole`: 4-lane board, invoice slip, delivery dock, holds, T+3), Partner Terms & Settlement two-column live desk (`PartnerTermsSection`). **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; `.tpd-stage` uses the same hero clamps as other product pages (`clamp(18rem, 42vh, 26rem)` tablet, `clamp(16rem, 38vh, 22rem)` phone) and reflows the desk (rail/floats/lists drop on phone so KPIs + charts fit); SearchFilter and InventoryAccess overlays stack instead of overlapping; order lanes and partner-terms columns stack; quotation builder / inventory / orders screens grow with content instead of clipping. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/marketplace-connectivity` is deep:** MarketplaceHero (left copy unchanged; right stage is a dark channel-ops mosaic — listing fan-out, overlapping channel tiles with density pips, sync timeline, ask-vs-floor comparator + 3-row conflict queue; primary green, `mkh-*`). Desktop (`.mkh-room` `clamp(22rem, 52vh, 34rem)`) is unchanged. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** `.mkh-room` uses auto height and `container: mkh / inline-size` so stacked tiles / timeline / queue are not clipped and the CHANNEL MESH / LIVE header is vertically centered in a 2.5rem bar; mosaic tiles reflow to a 2-col grid (no overlap); live-console copy stays above the stage; Channel / Listing / Price `.lc-screen` grow with content; capability board (`mcb-*`) stacks tiles below 640px and fits the coverage matrix without page overflow. Channel Status console (hub map + health), Listing Distribution console (create → push → qty sync → live fan-out), SyncDiagram, Price & conflict guard console (price ack + holds + delist), return-path capability board (`MarketplaceCapabilityBoard`: four equal tiles with sync status + coverage pips, channel×capability density matrix, inbound dock; original orders/delivery/errors/API-logs copy preserved), WorkflowSteps (POS → marketplaces → orders back). Own wrapper `MarketplaceLiveConsole` — does not extend broker `LiveConsoleVariant`. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/event-intelligence` is deep:** EventIntelHero (left copy unchanged; right stage is a dark AI forecast lens — P10–P90 probability cone, vertical model chips with freshness, confidence ring + sellout risk, comparable-event strip, Hold/Reprice/Release chips; primary green, `eih-*`). `.eih-room` is locked to the same frame as brokers/marketplace: `height`/`max-height: clamp(22rem, 52vh, 34rem)` (tablet `clamp(18rem, 42vh, 26rem)`; mobile `clamp(16rem, 38vh, 22rem)` like `.bh-wall-mask`). Event radar console (7-day onsale calendar heat strip + score-ranked radar queue + event signal panel), Event category board (`EventCategoryBoard`, `ecb-*`: catalog index + featured Football + five companion tiles with demand heat and onsale density; original six-category copy preserved), Demand & price signal console (event switcher, demand-column/ask-line chart to event day, comparable events, category bands), Venue & Category Intelligence two-column section (`VenueIntelligenceSection`: copy + mapped-sections table on the left, `ConsoleShell` SVG stadium map desk on the right), AI forecast cockpit (P10–P90 projection cone, sellout risk by band, 3-scenario board), WorkflowSteps (feeds → structured event → scoring → forecast → API). Own wrapper `EventIntelLiveConsole`. No overlapping/stacked-card treatment on this page — deliberate. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; stages flatten and grow with content so they fit the viewport; hero `.eih-room` keeps the existing clamps and reflows internals (model chips, cone, comps/scenarios — no overlay collision); category board stacks featured + companion tiles; venue SVG map scales to width. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/api` is deep:** ApiHero (left copy from the old PageHero — title and body unchanged; the former intro paragraph is the subhead; Become a seller + Request API Access). Right stage is a dark API documentation console (`apidoc-*`) — seven-product endpoint nav, GET/POST badge, path, params, JSON response, 200 OK, Bearer chip. `.apidoc-room` uses the same fixed frame: `clamp(22rem, 52vh, 34rem)` (tablet `clamp(18rem, 42vh, 26rem)`; mobile `clamp(16rem, 38vh, 22rem)`). Auth flow console (key → scopes → signed request → audit), ApiCards for the seven products, webhook delivery / payload explorer (signed POST, retries, inventory/listing/order/delivery events), WorkflowSteps (systems → APIs → sync back). Own wrapper `ApiLiveConsole`. Not a clone of travel/marketplace/event-intel/brokers heroes. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; auth/webhook `.lc-screen` grows with content; JSON/path wrap or scroll inside the console (no page overflow); hero `.apidoc-room` keeps the existing clamps and reflows internals (endpoint nav becomes a horizontal scroller with labels; JSON scrolls inside the response pane). ApiCards stay 1-col below 640px. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

Shared `WorkflowInfraCanvas` (used by WorkflowSteps on product How, `/products`, `/platform`, integrations, api, book-demo): step cards are glass surfaces (`wic-glass` / `wic-glass-host`) with no idle orbs. Hover reveals two static pill-rounded corner blobs — gray parked top-left (`top: -2.6rem; left: -2.85rem`), mint parked bottom-right (`right: -2.85rem; bottom: -2.6rem`, fill `#4be5ae59`) — clipped by overflow so copy stays clear. Inner copy, step numbers, SB mark, and perimeter packet dot stay above the shades. `prefers-reduced-motion` still shows the two shades on hover, with no opacity transition. Layout is `.ws-flow-grid--center`: flex-wrap + `justify-content: center` with locked 1/2/3/5-col widths so leftover rows (06–07 on a 7-card set) sit in the middle. Homepage HowItWorks is a different `.how-it-*` cinema.

**`/about` is the short commercial brief (Hero → Mission → Close; detailed remount reverted by user request, not a brokers clone, not a second FinalCTA):** dark unique `bh-hero` with extra top padding for the 4.5rem opaque nav. Left copy is **About SeatsBrokers / Built by Ticketing People** (30 years + fragmented inventory/data/marketplaces/payments/workflows). Right stage is `AboutJourneyWall` (`abt-*`) — seven official module rail, data-signal nodes, Experience+Data+AI+Automation strip. Hero CTAs: Become a Seller → `/become-a-seller`, Book a Demo → `/book-demo`, Talk to our team → `/contact`. **AboutMission sits immediately after the hero** (H2 **Our Mission**; intro is the brief mission sentence; SeatsGroup lead is infrastructure and distribution solutions; Manual→Automated transforms kept). Then **AboutClose** (pillars + the three CTAs). Unmounted leftover manifesto: Knowledge, Vision, Capabilities, Overview, LiveConsole, Audiences, Principles, Stack, Journey, Building, Future (files kept). FinalCTA stays in PageShell only. Copy in `about-page-data.ts`. Space Grotesk only. `--primary` green; no purple.

**`/book-demo` is a conversion page:** DemoHero (left copy; right stage is a dark session briefing desk — cycling London/New York/Dubai slots, 45-min agenda, attendee chips; `bdm-*`). Extra hero padding clears the overlay nav (including MacBook heights). What-you-will-see session itinerary (`DemoSessionBoard`, `bds-*`: 45-min proportional ruler, four numbered duration cards with outcomes, cycling “now in session” dock — not FeatureOrbit and not a clone of marketplace/event-intel boards), who-it-is-for two-track cards (brokers vs travel, plus marketplace/intel/API links), WorkflowSteps (3 steps), then DemoRequestForm (name, company, email, role, call window, message; client validation; mailto to `partners@seatsbrokers.com`; success state). FinalCTA off — the form is the close. Not a clone of brokers/marketplace/event-intel/API/about heroes.

**`/platform` is a stack overview (not a duplicate of `/brokers`):** PlatformHero (left copy; right stage is a hub + spine of five surfaces lighting up; `plt-*`). Dark module map with deep links to Brokers, Travel Partners, Marketplace Connectivity, Event Intelligence and API. WorkflowSteps for how the stack connects. FinalCTA via PageShell. Platform remains `hidden: true` in header nav; Hero “Explore Our Platform” and the route still work. Footer Company column includes Become a seller (Platform Overview stays out of the footer).

**`/contact` is a compact dark two-column `bh-hero`:** left CONTACT + Talk to Our Team + Become a seller (`/brokers`); right HUD photo in `.ct-hero-stage` (`object-fit: cover`, clipped to `clamp(22rem, 52vh, 34rem)` desktop — never PNG height). Left black vignette; no green wash.

Other product routes (`/market-analytics`, `/ai-pricing`, `/integrations`) still use PageHero + FeatureGrid / WorkflowSteps / SplitPanel / ApiCards — no live consoles yet.

Agent memory: `MEMORY.md` + `.cursorrules` + `docs/agent/` (PROJECT_MAP, CHANGE_MAP, ARCHITECTURE, DATA_FLOW, CONVENTIONS, DECISIONS, KNOWN_ISSUES). Detail docs in this folder (`DESIGN_SYSTEM`, `COMPONENT_MAP`, page plans) are referenced, not duplicated.

## In progress / next up

No active build task. Natural next work if asked:

- Wire `LiveConsole variant="pos"` into `/brokers` (PosConsole is already built)
- Bring `/ai-pricing` or `/market-analytics` up to live-console depth (event intelligence, API, book-demo and platform overview are done)
- Delete or use `MarketPathScroll` (orphan landing component)
- Resolve LiveConsole stubs `payments` / `scheduling` (payments already has its own section; USDT payouts are `cryptoPayouts`)

## Open decisions

- **PosConsole** is implemented and registered on `LiveConsole` but not rendered in `src/routes/brokers.tsx`.
- **LiveConsole `payments` / `scheduling`** are `null`; payments is `PaymentInfrastructureSection` instead. USDT/crypto payouts are a separate mounted variant, `cryptoPayouts`.
- **`MarketPathScroll`** is unused.
- **`.dark` class** is shadcn default (purple-blue) and unused; dark *bands* use `bg-dark` (`--dark` green-black). Do not enable class-based dark mode without retokenizing.
- **Type:** one marketing family — Space Grotesk (`--font-sans` / `--font-display`). Playfair Display and Inter are gone. Italic is `font-style: italic` in Space Grotesk. JetBrains Mono stays for data/eyebrows only.
- Travel Margin Management is a local `SplitPanelLocal` in `travel-partners.tsx`, not the shared `SplitPanel` component.
- **Broker onboarding unknowns (do not invent in UI):** `[CONFIRM: typical setup time Dubai/London/India/NY]`, `[CONFIRM: local desk hours Dubai/London/India/NY]`, `[CONFIRM: incorporate-from-zero vs connect-existing]`, `[CONFIRM: setup cost]`, `[CONFIRM: partner sub-account cap]`, `[CONFIRM: partners in a different region than the primary company]`. Shipped copy uses desk-confirm / we-do-not-publish-here language.

## Already decided — do not re-litigate

- Brand name: SeatsBrokers
- Primary color: `#198754` (implemented as `oklch(0.548 0.117 158.2)` — do not swap to a different green)
- No competitor product names/taglines reused verbatim
- Live Console pattern is fixed (see docs/DESIGN_SYSTEM.md) — don't reinvent it per section
- Brokers, travel, marketplace, event-intelligence and API consoles stay in separate folders (`pages/brokers` vs `pages/travel` vs `pages/marketplace` vs `pages/event-intelligence` vs `pages/api`); do not extend `LiveConsoleVariant` with types from another page. `AboutLiveConsole` is the data→intelligence pipeline on `/about` (own wrapper).
- `/event-intelligence` does not use the overlapping / stacked-card treatment — clean side-by-side and in-flow layouts only
- PageShell already renders FinalCTA globally — do not add a second page-level CTA
- Lovable-connected git: no force-push / history rewrite (`AGENTS.md`)
