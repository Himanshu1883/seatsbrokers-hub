# Project Status
(Living doc — update after every session, read first in every new one.)

Last updated: 2026-08-17 (nav + footer Platform column restored descriptive labels: Marketplace Connectivity, Event Intelligence, API — not SeatsMarket™ / SeatsIntel™ / SeatsLink™)

## Current state

**`/legal` is one legal page (not three sites, not a product console):** full-width dark `bh-hero` copy banner (no 46rem prose column, no right-stage chapter rail; `.legal-shell` uses `padding-inline: clamp(1rem, 3vw, 2rem)`). Light body is three equal tab cards in a row (Privacy | Terms | Cookies) plus one full-width policy panel; desktop policy blocks fill 2–3 columns. Hashes `/legal#privacy`, `/legal#terms`, `/legal#cookies` select the matching tab so footer links land correctly. Copy is limited to project facts (SeatsBrokers, B2B brokers/travel partners, London · New York · Dubai, sales@ and partners@, £). No invented company number. Footer `footerLegal` stays on this page; API Terms stays `/api`. CSS: `.legal-*` only.

**`/faq` is a FAQ page (not a product-console stack):** FaqHero reuses dark `bh-hero` / `bh-stage` chrome (gradient, orb, copy icon, extra top/bottom padding so copy clears the overlay nav including MacBook). Left copy inverts to light type (`bh-title` / `bh-body`) with `--primary` eyebrow and italic “We've got answers.” (Space Grotesk, not a second face). Right stage stays unique: a dark-glass 06 TOPICS rail (green indices; not a brokers/marketplace dashboard). Accordion and help strip unchanged (light list, then dark mailto band). Six questions in `FaqAccordion` (shadcn Accordion, `faq-*`): inventory types, API for brokers and travel partners, authenticity, travel-agency delivery, bulk/holds, platform charges in £. `FaqHelpStrip` is a dark still-need-help band with mailto cards to `sales@seatsbrokers.com` (brokers, marketplaces & API) and `partners@seatsbrokers.com` (travel partners) — replies within one business day; no Book a Demo. Copy lives in `faq-data.ts`. FinalCTA via PageShell only (Become a seller → `/brokers`). Footer Resources “FAQs” now points to `/faq` (was `/contact`); header nav does not list FAQ. **Phone / small-tablet (`≤639px`, 320–430):** topic index stacks 1-col under 430px and 2-col to 639px; accordion triggers `min-height` 44px with wrapping questions; emails `overflow-wrap: anywhere`; no horizontal overflow. Tablet (`640–1023`): 3-col topic chips, two help cards. Desktop (`≥1024px`): two-column hero (copy + vertical topic ledger) and two-column help strip. Not a clone of brokers/PageHero.

**Homepage who-we-serve (`StickyScrollShowcase`, `sss-*`):** section titles/H2s are sentence case (no Tailwind `uppercase`); kickers stay `section-eyebrow`. The sticky screenshot column is now four unique light `ConsoleShell` mini-consoles (`StickyScrollConsoles.tsx`) bound 1:1 to the audience steps — broker distribution (channel pips + listing table + ask spark), travel quote desk (£ cost / margin / client + PDF/WhatsApp/email), marketplace sync (ingest→order rail, ask vs floor, channel lag), partner API bridge (endpoint cycle + signed JSON). All four share a fixed `--sss-console-h` on `.sss-stage` (phone `clamp(20.5rem, 56svh, 24.5rem)`, default/mobile `clamp(22.5rem, 58svh, 28rem)`, tablet `clamp(26rem, 48vh, 30rem)`, desktop `clamp(28rem, 58dvh, 34rem)`) so sticky swaps do not jump; shorter broker content flex-fills `.lc-body` / `.sss-work`. Desktop (`≥1024px`) keeps copy scrolling and the right console sticky, sliding with the active step; below `lg` each console stacks above its copy (readable at 375px). Live ticks run only on the active/in-view console; `prefers-reduced-motion` freezes them and still shows the chrome. Not a clone of brokers/marketplace/travel heroes.

**Homepage hero backgrounds:** three stadium photos in `src/assets/hero-stadium-{1,2,3}.*` (console 0→1, 1→2, 2→3) crossfade on the same `active` index / `SLIDE_MS` 2100 as the light HUDs; the overlay is a black vignette (center starts at `0.54` black, 38% `0.60`, mid `0.72`, edges `0.90`; plus top/left linear — no `primary-deep` green wash). **Homepage hero console (`HeroDashboardTilt`, `hero-hud-*`):** the tilt stage is light glass (white/off-white panels, gray chrome, dark text, `--primary` accents; still 3D-tilted) and renders one of three unique consoles bound 1:1 to the hero slide (auto-advance `SLIDE_MS` 2100, independent of typewriter) — platform layers + scrolling system feed (slide 1), listing × channel distribution matrix + export-close bar (slide 2), partner margin/quote desk with £ client price (slide 3) — swapping on auto-advance with a quick jump-up slide-in; the typewriter line reserves its longest phrase (ghost line-box) and all three consoles share the same fixed `28rem` frame, so no slide change resizes the hero; mobile `100svh` lock, console `--hero-console-scale` `0.85` (`0.82` under 700px height) filling the leftover flex slot (`flex: 1 1 0`, no `--hero-scene-h` cap); shallow 3D tilt kept; CTAs sit in a 2-column wrap on mobile (not stacked full-width rows); phone copy is pill + headline + typewriter + short body + CTAs (long lead/dense/tags still hidden; type shrinks under 700px instead of `display:none`).

Marketing site is live as a TanStack Start app. Homepage (`/`) is a full landing narrative (Hero through Testimonials) wrapped in PageShell (Nav + FinalCTA + Footer). **Phone/small-tablet homepage** (`≤639px` and below `lg`) uses stacked layouts: homepage hero is locked to `100svh` (nav ~4.5rem spacer; reduced type — pill + headline + typewriter + short body + 2-column CTAs; long lead, dense body/bullets and tags hidden; headline `line-height` 1.25 + padding so iOS does not crop ascenders; tilt console fills the leftover flex slot at `--hero-console-scale` `0.85` / `0.82` under 700px height; next section does not peek); NetworkConstellation is a `100dvh` section at `≥1024px` (globe + rail + copy clamped to fit one viewport, including 1280/1440/1512/1680/1920/2560; mobile stays auto-height); chips/rail/nodes/dots still click to a stage and restart a 1.8s auto-advance (keeps running on hover; `prefers-reduced-motion` off) — no 340vh pin or scroll-scrub; Marketplaces is a full-width hub (SeatsBrokers logo in a white squircle; left/right glass pods fill the row and stream items inward — left LTR, right RTL — Events / Inventory / Listings in, Marketplaces / Travel partners / POS out; stacks top→logo→bottom below `lg`, `prefers-reduced-motion` is static); FeatureOrbit is a list; JourneyNumbers is the card stack; TwoTrack cards straighten once on in-view (no GSAP pin/scrub); Toolkit/Globe/StickyScroll/HowItWorks drop sticky/min-viewport cinema and stack. Desktop HowItWorks (`≥1024px`) is a two-column cinema: left step cards scroll, right product screenshot stays sticky and swaps with the active step; wheel over the screenshot scrolls the page (no nested overflow-y-auto). Desktop (`≥1024px`) homepage hero keeps the two-column cinematic layout: copy and console sit as one centered unit (`40rem` + `44rem` columns, `container-page` 94rem, tops aligned); the section is a flex column (`min-height: 100dvh`, height auto — not a shrinking `minmax(0,1fr)` grid) with `.hero-fit-offset` `clamp(6.75rem, 5.25rem + 4vh, 8.5rem)` so content clears the fixed nav; **laptop band 1024–1919px** (`1512×982`, `1440×900`, `1680×1050`) zooms `.hero-fit-zoom` (offset + copy + console + CTAs + dots) to `0.85` so the full hero fits without bottom crop (`@supports not (zoom)` uses `scale(0.85)` from top center); **≥1920px** leaves the hero at 100% (no second 0.85 on top of any large-screen html zoom) and only console-scales under 980/900/820px height; dots `padding-bottom: clamp(1.75rem, 3vh, 2.75rem)`; nav uses the same 94rem max-width as page content. Mobile `100svh` lock, `4.5rem` offset, and `display: contents` on `.hero-fit-zoom` are unchanged. Cinematic layouts otherwise unchanged. Header nav (`src/components/landing/Nav.tsx`, links in `src/content/site.ts`) highlights the current route in brand `--primary` green (underline + color) on desktop and mobile. Visible product links are **Marketplace Connectivity** (`/marketplace-connectivity`), **Event Intelligence** (`/event-intelligence`) and **API** (`/api`); For Brokers and About stay as-is. Footer Platform column uses the same three descriptive labels (module TM names stay on page heroes, not chrome). Official module names and taglines live in `src/content/modules.ts`. **Phone / tablet nav (`<1024px`):** logo left + 44px hamburger right in the same 4.5rem bar (hero offset unchanged); full-bleed sheet below the bar (right sheet + dimmed backdrop from 768px) with scrollable links, pinned Login (no Become a seller in nav), body-scroll lock, and close on route change / Escape / backdrop; sheet focuses the dialog (not For Brokers) and iOS tap/focus rings are off on coarse pointers; Platform and Travel Partners stay hidden; About is visible. The header bar is always opaque `var(--background)` (white; fixed overlay, no glass/blur) with a muted `--border` hairline and dark text (active `--primary`). The PNG wordmark on the header is the original file (not `.brand-logo-on-dark`); the dark footer still inverts via `.brand-logo-on-dark`; FeatureOrbit (white disc) keeps the original file. Matching is exact or nested prefix (`pathname === to || startsWith(\`${to}/\`)`); the logo only matches `/` (`activeOptions.exact`). **Platform and Travel Partners are hidden from the visible nav** (`hidden: true` on those links) — `src/routes/platform.tsx` and `src/routes/travel-partners.tsx` still serve their URLs. About is a visible nav link to `/about`. Footer (`footerColumns`) no longer lists Platform Overview, Integrations, Careers, Market Analytics, or AI Pricing; those routes still exist. Primary chrome CTA is **Become a seller** (`ctas.becomeSeller` → `/brokers`); Book a Demo is commented out in `site.ts` and `/book-demo` is unlinked from chrome (route kept). Footer partners line is `Partners: sales@seatsbrokers.com · partners@seatsbrokers.com` (mailto); social icons are commented out. Product-page heroes (`.bh-hero` and the first `.section-curve` in `<main>`, including `PageHero`) use `margin-top: clamp(0.25rem, 0.65vw, 0.5rem)` to match `.section-curve`’s side gutter so the rounded card is not flush to the fixed nav; the homepage cinematic hero (`section-curve-hero`) stays full-bleed.

**`/brokers` is deep:** BrokersHero wall, Event Catalog console, Inventory Management console, SyncDiagram, SectionConnectors, Market Intelligence + AI Pricing live consoles, WorkflowSteps (POS → API), ApiCards, Payment Infrastructure dashboard. Market Intelligence (`mi-*`) stacks ASK LADDER and SIGNAL FEED as separate regions: the grid flexes and clips, the feed is reserved below with its own overflow — ladder rows no longer paint through the feed. **Phone / small-tablet (`≤639px`, and overflow-clip through `1023px`):** copy stacks above live-console tilt stages; stages flatten and clip so they fit the viewport; hero wall uses existing `.bh-wall-mask` clamps (`clamp(16rem, 38vh, 22rem)`); Event Catalog / Market Intelligence `.lc-screen` heights are clamped; WorkflowSteps cards stay single-column. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/travel-partners` is deep:** TravelPartnersHero (left copy unchanged; right stage is a dark partner-desk analytics dashboard — KPIs, £ earnings float, weekly bookings + inventory fill, progress lists, order-status float; primary green, `tpd-*`), Inventory Access console (markets + partner view + margin preview), Inventory Access stacked-card showcase (catalog + detail overlay), Search filter zoom (diagonal glass cards: unfiltered list → filtered event; console-density tables inside both cards), Margin Management (£500 / 10% / £550), Quotation Builder console, Order & Delivery FeatureGrid + fulfillment desk (`PartnerOrdersConsole`: 4-lane board, invoice slip, delivery dock, holds, T+3), Partner Terms & Settlement two-column live desk (`PartnerTermsSection`). **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; `.tpd-stage` uses the same hero clamps as other product pages (`clamp(18rem, 42vh, 26rem)` tablet, `clamp(16rem, 38vh, 22rem)` phone) and reflows the desk (rail/floats/lists drop on phone so KPIs + charts fit); SearchFilter and InventoryAccess overlays stack instead of overlapping; order lanes and partner-terms columns stack; quotation builder / inventory / orders screens grow with content instead of clipping. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/marketplace-connectivity` is deep:** MarketplaceHero (left copy unchanged; right stage is a dark channel-ops mosaic — listing fan-out, overlapping channel tiles with density pips, sync timeline, ask-vs-floor comparator + 3-row conflict queue; primary green, `mkh-*`). Desktop (`.mkh-room` `clamp(22rem, 52vh, 34rem)`) is unchanged. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** `.mkh-room` uses auto height and `container: mkh / inline-size` so stacked tiles / timeline / queue are not clipped and the CHANNEL MESH / LIVE header is vertically centered in a 2.5rem bar; mosaic tiles reflow to a 2-col grid (no overlap); live-console copy stays above the stage; Channel / Listing / Price `.lc-screen` grow with content; capability board (`mcb-*`) stacks tiles below 640px and fits the coverage matrix without page overflow. Channel Status console (hub map + health), Listing Distribution console (create → push → qty sync → live fan-out), SyncDiagram, Price & conflict guard console (price ack + holds + delist), return-path capability board (`MarketplaceCapabilityBoard`: four equal tiles with sync status + coverage pips, channel×capability density matrix, inbound dock; original orders/delivery/errors/API-logs copy preserved), WorkflowSteps (POS → marketplaces → orders back). Own wrapper `MarketplaceLiveConsole` — does not extend broker `LiveConsoleVariant`. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/event-intelligence` is deep:** EventIntelHero (left copy unchanged; right stage is a dark AI forecast lens — P10–P90 probability cone, vertical model chips with freshness, confidence ring + sellout risk, comparable-event strip, Hold/Reprice/Release chips; primary green, `eih-*`). `.eih-room` is locked to the same frame as brokers/marketplace: `height`/`max-height: clamp(22rem, 52vh, 34rem)` (tablet `clamp(18rem, 42vh, 26rem)`; mobile `clamp(16rem, 38vh, 22rem)` like `.bh-wall-mask`). Event radar console (7-day onsale calendar heat strip + score-ranked radar queue + event signal panel), Event category board (`EventCategoryBoard`, `ecb-*`: catalog index + featured Football + five companion tiles with demand heat and onsale density; original six-category copy preserved), Demand & price signal console (event switcher, demand-column/ask-line chart to event day, comparable events, category bands), Venue & Category Intelligence two-column section (`VenueIntelligenceSection`: copy + mapped-sections table on the left, `ConsoleShell` SVG stadium map desk on the right), AI forecast cockpit (P10–P90 projection cone, sellout risk by band, 3-scenario board), WorkflowSteps (feeds → structured event → scoring → forecast → API). Own wrapper `EventIntelLiveConsole`. No overlapping/stacked-card treatment on this page — deliberate. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; stages flatten and grow with content so they fit the viewport; hero `.eih-room` keeps the existing clamps and reflows internals (model chips, cone, comps/scenarios — no overlay collision); category board stacks featured + companion tiles; venue SVG map scales to width. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

**`/api` is deep:** ApiHero (left copy from the old PageHero — title and body unchanged; the former intro paragraph is the subhead; Become a seller + Request API Access). Right stage is a dark API documentation console (`apidoc-*`) — seven-product endpoint nav, GET/POST badge, path, params, JSON response, 200 OK, Bearer chip. `.apidoc-room` uses the same fixed frame: `clamp(22rem, 52vh, 34rem)` (tablet `clamp(18rem, 42vh, 26rem)`; mobile `clamp(16rem, 38vh, 22rem)`). Auth flow console (key → scopes → signed request → audit), ApiCards for the seven products, webhook delivery / payload explorer (signed POST, retries, inventory/listing/order/delivery events), WorkflowSteps (systems → APIs → sync back). Own wrapper `ApiLiveConsole`. Not a clone of travel/marketplace/event-intel/brokers heroes. **Phone / small-tablet (`≤639px`, overflow-clip through `1023px`):** copy stacks above live-console stages; auth/webhook `.lc-screen` grows with content; JSON/path wrap or scroll inside the console (no page overflow); hero `.apidoc-room` keeps the existing clamps and reflows internals (endpoint nav becomes a horizontal scroller with labels; JSON scrolls inside the response pane). ApiCards stay 1-col below 640px. Desktop (`≥1024px`) is unchanged. No horizontal page overflow at ~375px.

Shared `WorkflowInfraCanvas` (used by WorkflowSteps on brokers, marketplace-connectivity, event-intelligence, platform, ai-pricing, integrations, api): step cards are glass surfaces (`wic-glass` / `wic-glass-host`) with no idle orbs. Hover reveals two static pill-rounded corner blobs — gray parked top-left (`top: -2.6rem; left: -2.85rem`), mint parked bottom-right (`right: -2.85rem; bottom: -2.6rem`, fill `#4be5ae59`) — clipped by overflow so copy stays clear. Inner copy, step numbers, SB mark, and perimeter packet dot stay above the shades. `prefers-reduced-motion` still shows the two shades on hover, with no opacity transition.

**`/about` is a company manifesto page (not a brokers clone, not a second FinalCTA):** dark unique `bh-hero` with extra top padding for the 4.5rem opaque nav. Left copy is About SeatsBrokers / Intelligent Technology for the Future of Ticketing (30 years + mission). Right stage is an intelligence core (`abt-*`) — ecosystem rail (SeatsAI, SeatsTrade, SeatsPay, SeatsAPI, SeatsData, SeatsAutomation), data-signal nodes, Experience+Data+AI+Automation strip. Chapters after: Knowledge, Vision, six AI capabilities, ecosystem board, Collect→Act Live Console, audiences, Why SeatsBrokers, technology stack, five-era journey, intelligent OS, mission transforms, future pairs, then a close band (AI + Technology + Ticketing) with Explore Our Technology → `/platform`, Become a Partner → `/brokers`, Talk to Our Team → `/contact`. No Book a Demo. FinalCTA stays in PageShell only. Copy in `about-page-data.ts`. Space Grotesk only. `--primary` green; no purple. Our Journey ledger draws one parent line at `--abt-journey-line-x` (`calc(2.2rem + 1px)`, center of the 2.4rem index column) so gap segments no longer sit left of the numbers.

**`/book-demo` is a conversion page:** DemoHero (left copy; right stage is a dark session briefing desk — cycling London/New York/Dubai slots, 45-min agenda, attendee chips; `bdm-*`). Extra hero padding clears the overlay nav (including MacBook heights). What-you-will-see session itinerary (`DemoSessionBoard`, `bds-*`: 45-min proportional ruler, four numbered duration cards with outcomes, cycling “now in session” dock — not FeatureOrbit and not a clone of marketplace/event-intel boards), who-it-is-for two-track cards (brokers vs travel, plus marketplace/intel/API links), WorkflowSteps (3 steps), then DemoRequestForm (name, company, email, role, call window, message; client validation; mailto to `partners@seatsbrokers.com`; success state). FinalCTA off — the form is the close. Not a clone of brokers/marketplace/event-intel/API/about heroes.

**`/platform` is a stack overview (not a duplicate of `/brokers`):** PlatformHero (left copy; right stage is a hub + spine of five surfaces lighting up; `plt-*`). Dark module map with deep links to Brokers, Travel Partners, Marketplace Connectivity, Event Intelligence and API. WorkflowSteps for how the stack connects. FinalCTA via PageShell. Platform remains `hidden: true` in header nav; Hero “Explore Our Platform” and the route still work. Footer Company column includes Become a seller (Platform Overview stays out of the footer).

**`/contact` is a compact dark two-column `bh-hero`:** left CONTACT + Talk to Our Team + Become a seller (`/brokers`); right HUD photo in `.ct-hero-stage` (`object-fit: cover`, clipped to `clamp(22rem, 52vh, 34rem)` desktop — never PNG height). Left black vignette; no green wash.

Other product routes (`/market-analytics`, `/ai-pricing`, `/integrations`) still use PageHero + FeatureGrid / WorkflowSteps / SplitPanel / ApiCards — no live consoles yet.

Agent memory: `MEMORY.md` + `.cursorrules` + `docs/agent/` (PROJECT_MAP, CHANGE_MAP, ARCHITECTURE, DATA_FLOW, CONVENTIONS, DECISIONS, KNOWN_ISSUES). Detail docs in this folder (`DESIGN_SYSTEM`, `COMPONENT_MAP`, page plans) are referenced, not duplicated.

## In progress / next up

No active build task. Natural next work if asked:

- Wire `LiveConsole variant="pos"` into `/brokers` (PosConsole is already built)
- Bring `/ai-pricing` or `/market-analytics` up to live-console depth (event intelligence, API, book-demo and platform overview are done)
- Delete or use `MarketPathScroll` (orphan landing component)
- Resolve LiveConsole stubs `payments` / `scheduling` (payments already has its own section)

## Open decisions

- **PosConsole** is implemented and registered on `LiveConsole` but not rendered in `src/routes/brokers.tsx`.
- **LiveConsole `payments` / `scheduling`** are `null`; payments is `PaymentInfrastructureSection` instead.
- **`MarketPathScroll`** is unused.
- **`.dark` class** is shadcn default (purple-blue) and unused; dark *bands* use `bg-dark` (`--dark` green-black). Do not enable class-based dark mode without retokenizing.
- **Type:** one marketing family — Space Grotesk (`--font-sans` / `--font-display`). Playfair Display and Inter are gone. Italic is `font-style: italic` in Space Grotesk. JetBrains Mono stays for data/eyebrows only.
- Travel Margin Management is a local `SplitPanelLocal` in `travel-partners.tsx`, not the shared `SplitPanel` component.

## Already decided — do not re-litigate

- Brand name: SeatsBrokers
- Primary color: `#198754` (implemented as `oklch(0.548 0.117 158.2)` — do not swap to a different green)
- No competitor product names/taglines reused verbatim
- Live Console pattern is fixed (see docs/DESIGN_SYSTEM.md) — don't reinvent it per section
- Brokers, travel, marketplace, event-intelligence and API consoles stay in separate folders (`pages/brokers` vs `pages/travel` vs `pages/marketplace` vs `pages/event-intelligence` vs `pages/api`); do not extend `LiveConsoleVariant` with types from another page. `/about` has its own `AboutLiveConsole` for the data→intelligence pipeline only.
- `/event-intelligence` does not use the overlapping / stacked-card treatment — clean side-by-side and in-flow layouts only
- PageShell already renders FinalCTA globally — do not add a second page-level CTA
- Lovable-connected git: no force-push / history rewrite (`AGENTS.md`)
