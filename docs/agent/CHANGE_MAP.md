# Change Map

Use this before grep. If the path is wrong, trust the code and fix this file.

Component inventory: `docs/COMPONENT_MAP.md`. Page depth: `docs/PROJECT_STATUS.md`.

## Site chrome

| Request | Primary | Related |
|---|---|---|
| Navbar links / hide Platform & Travel Partners | `src/content/site.ts` `navLinks` | Visible: Platform, Products (`/products`), Integrations, API, About. Hidden: For Brokers, B2B Partners, Marketplace Connectivity, Event Intelligence. Right chrome: Login, Become a Seller (outline). **Book a Demo hidden from nav** (desktop + mobile sheet commented in `Nav.tsx` 2026-08-25) — Hero / FinalCTA / `/book-demo` still open `DemoModal`. `Nav.tsx`, `.site-nav-*` in `styles.css` |
| Nav logo lockup | `Nav.tsx` `src/assets/SeatsBrokers-favicon.svg` + `.site-nav-logo-fit` / `.site-nav-logo-img` | Vector lockup (chevrons + wordmark + tagline). viewBox `38 49 491 76` trims empty canvas only (ink `40.9–526.3 × 51.4–122.8`, not clipped). Same CSS size at 375 / 768 / 1024 / 1512: **2.5rem** tall, width `calc(2.5rem * 491 / 76)`. ≤359px / 320px bar: **2.25rem**. No JPEG/PNG, no `translate` crop. Bar 4.5rem. Footer `logo-sb.png` does not share this class |
| Nav solid light bar / header CTAs | `Nav.tsx`, `.site-nav-bar` in `styles.css` | Opaque `var(--background)` + muted `--border`. Right: Login (text), Become a Seller (outline). Book a Demo removed from desktop + mobile sheet (commented — restore in `Nav.tsx` if needed). |
| Active nav style | `Nav.tsx` | — |
| Logo visibility on dark | Footer only (`.brand-logo-on-dark`); header uses SVG lockup on light bar | `.brand-logo-on-dark*` in `styles.css` |
| Favicon / document icon | `src/routes/__root.tsx` `head.links` | `public/seatsbrokers-mark.svg` — chevron mark only (viewBox crop of the official lockup; well-formed XML, no C0 control chars — editor preview rejects illegal XML). Nav `src/assets/SeatsBrokers-favicon.svg` (full lockup, viewBox `38 49 491 76`). Footer `src/assets/logo-sb.png`. Full lockup also at `public/SeatsBrokers-01 (1).svg` |
| Footer columns | `site.ts` `footerColumns` | Platform, Products (seven modules → `/products/seats*`), Company, Support. `Footer.tsx`. Brand line `brand.groupLine`. Company column: About, Contact, Book a Demo, Become a Seller |
| Footer legal links | `site.ts` `footerLegal` | Privacy / Terms / Cookie Policy / Compliance → `/legal#privacy` `#terms` `#cookies` `#compliance`. API Terms still `/api` |
| FAQ page | `src/routes/faq.tsx` | `pages/faq/`, `faq-data.ts`, `.faq-*` in `styles.css` (dark `bh-hero` + topics glass); footer Resources “FAQs” → `/faq` |
| Legal page | `src/routes/legal.tsx` | `pages/legal/`, `legal-data.ts`, `.legal-*` in `styles.css` (short dark `bh-hero`; full-width 4-col tabs + panel; hashes `#privacy` `#terms` `#cookies` `#compliance`) |
| Global CTA band | `src/components/landing/FinalCTA.tsx` | PageShell `showFinalCta`. Phase 2: **Book a Demo** (primary) + **Become a Seller** (outline) + **Login** text (`ctas.login` still `/contact` until app host exists). Book a Demo opens `DemoModal` (not navigate). |
| Book a Demo modal | `landing/DemoModal.tsx` (`DemoModalProvider` + `useDemoModal`) | Mounted in `PageShell`. Opens from Nav / FinalCTA / Hero. **Single-column stack** (`data-layout="stack"`): top bar (mint calendar + “Book a demo” + X) → title (mint SeatsBrokers) + subhead → 4-col USP strip → icon form (name/email/phone\|company/business select) → 2-col product cards (`moduleList` + API, green border + check on select) → Tell us more → Prefer `/book-demo` → mint “Send message →” → lock privacy. Panel `max-height` ≤100svh; ONLY `.demo-modal-body` thin-scrolls. Supersedes prior `data-layout="ref"` split. `submitLead`. Full `/book-demo` unchanged. |
| Page wrapper / gaps | `PageShell.tsx` | Wraps `DemoModalProvider`. `.section-curve` in `styles.css` |
| Hero top inset | `.bh-hero` `padding-top: clamp(5.75rem, 8vh, 7.5rem)` (4.5rem nav + gap). Homepage `.hero-fit-offset` `calc(4.5rem + 1.75rem)` mobile / desktop `clamp(7.5rem, 6.25rem + 4.5vh, 9.25rem)` / laptop 1024–1919 tall windows `clamp(8.65rem, 7.25rem + 5.5vh, 11rem)` (inside `.hero-fit-zoom` 0.85) / **Air / short laptop ≤1100dvh** `calc(4.5rem + 0.7rem)` with zoom off | unique `abt/bdm/plt/faq/legal/slr` inherit top; `.ct-hero-copy` on desktop; `PageHero` unused (kept); not homepage `.section-curve-hero` |

## Homepage `/`

| Request | Primary | Related |
|---|---|---|
| Section order | `src/routes/index.tsx` | **Reference structure 2026-08-24:** Hero → ProcessBento (`#partner-process` ribbon) → SellerTools (`#sellers` engine) → FeatureOrbit (`#platform` seven products) → ToolkitShowcase (`#platform-toolkit` connect once) → Marketplaces (`#connectivity`) → MarketIntelligence (`#market-intelligence`) → TwoTrack (broker vs B2B). FinalCTA is PageShell. **TravelTools** on `/products/seatsdeal` only. **Unmounted (files kept):** HowItWorks, TravelTools, JourneyNumbers, StickyScrollShowcase, Stats, NetworkConstellation, GlobalReach, GlobeScrollSection, PartnerProductShowcase, Testimonials. Book a Demo primary; Become a Seller secondary. Our mint/light theme — not the purple mock. |
| Hero typewriter | `landing/Hero.tsx` | `useTypewriter`; Phase 2 pitch: **Technology powering / professional ticket businesses.** (mint on type line). **2026-08-25 cinema + bars:** stadium veil restored to the earlier lighter vignette (center `0.54`). Four highlights sit **under the CTAs in the left copy column** (2×2). Bottom of hero is the **7-step Discover→Settle rail**. **Laptop / MacBook Air (1024–1919 × ≤1100dvh):** hero locks to `100dvh`, zoom/scale off, `--hero-card-h` from leftover `calc(100dvh - 23.5rem)` so the 7-rail is not cropped (Safari ignores `zoom:0.85`). ≥1920 unchanged. Pill **One platform.**; CTAs Book a Demo (`DemoModal`) + Become a Seller. ProcessBento still owns the live 01–07 hop below. |
| Hero console (3 ConsoleShell desks) | `landing/HeroDashboardTilt.tsx` | Light `ConsoleShell` desks inside 3D tilt stage (`slide` / `swapKey`, `SLIDE_MS` 2100). Slide 1 operations hub; slide 2 Discover→Settle workflow (`seatsbrokers / platform / overview` + Live); slide 3 seven-product command + SeatsPulse™ you-decide rec. `.hero-desk-*` in `styles.css`; desktop `--hero-card-h` **35rem**. Rest pose `--tilt-x: 8deg` / `--tilt-y: -16deg` (pointer fine modulates around rest). Qualitative stats; £ in illustrative rows. **Phones ≤639px:** flatten (`transform: none`); `--hero-console-scale: 1`; `min-width: 0`. **Tablet 640–1023:** shallower tilt (~5deg / -9deg). Pack desk to leftover card height. |
| Feature orbit | `landing/FeatureOrbit.tsx` | `.feature-orbit-*`, `--fo-*`, `.feature-orbit-section`. **CSS restore 2026-08-22:** `.feature-orbit-cta` (44px Explore links), `.feature-orbit-node-btn` / `-title` / `-tagline`, gold/olive accents, stacked `.feature-orbit-mobile-*` — recovered from the prior agent append (never committed; wiped by a later `styles.css` collision). Seven official modules (`modules.ts` + `productHrefs`): Intel → Source → Pulse → Link → Market → Deal → Funds. Auto-advances active index `0→6` every **2100ms** while in view (`useInView` `once: false`, threshold 0.16); solid spoke + detail follow. Pause on orbit/card hover or press; resume 450ms after leave. `prefers-reduced-motion` freezes on current card (no interval). Click still jumps to a card and restarts the dwell. Desktop detail popover + mobile stacked list each have an **Explore {Product}™** `SiteLink` (44px tap). Mobile is a stacked title/body/CTA list (no shared detail slot). Desktop rings stay in-flow: no 7.5rem `.feature-orbit-detail-reserve`, no `100svh` / 340vh pin. Lower-half nodes open the detail upward so popovers stay inside the square; seven pagination dots sit `1.25rem` after the orbit (`margin-bottom: 0`). Section is `height: auto` / `overflow: visible` with `padding-block: 4rem 2.25rem` (`5rem 2.5rem` from 640px) — top unchanged, bottom was 4rem / 5rem. |
| Network constellation | `landing/NetworkConstellation.tsx` | `GlobeCanvas` idle orbit; click stages (`.nc-*`); `100svh` lock at `≥1024px` (not `dvh`); globe square + reserved console title/body/metric so 1.8s auto-advance never shifts page height; no hover pause; no 340vh pin |
| Marketplaces strip | `landing/Marketplaces.tsx` | Full-width hub: center logo + inward glass pods with dual marquees (`.mps-hub-*`). Left LTR / right RTL into the logo. Hub disc is `--mps-hub-slot` **5.5rem / 6.25rem** (88/100px; was 7rem / 8rem). 3-col stays `minmax(0,1fr) var(--mps-hub-slot) minmax(0,1fr)`. `.mps-hub-logo` sizes to the slot; lockup crop **82%** of the disc, **no `translateX`** (was 82% + `translateX(5%)`, which packed the wordmark on the right rim). Ink crop stays `translate(-7.333%, -29.348%)` on `.mps-hub-logo-img`. Joins use `right/left: -0.35rem` (column-gap) with rail `overflow: visible` so connectors dock on the circle. |
| Market intelligence (homepage §7) | `landing/MarketIntelligence.tsx` `#market-intelligence` | Left marketing copy (H2 **Better Data** in primary; qualitative “AI monitoring live market signals”; four feature icon rows) + dark dense Demo `ConsoleShell` desk (`.mihp-*`): narrow icon rail, Live + 1D/7D/30D/Custom + Filters toolbar, 5 KPI cards (incl. demand/AI confidence gauge), price-trend chart, inventory heatmap, AI recommendations, market comparison table, demand forecast, top movers. Illustrative £/row numbers Demo-stamped — no “4.2M+” marketing claim. Soft in-view rec focus + chart draw; pause on hover; `prefers-reduced-motion` freezes. CTAs Explore SeatsIntel™ / Explore SeatsPulse™. Mobile: copy above console; stage may scroll-x. Corner shell: `section-curve` + inner `.mihp-shell` (`overflow: hidden`, `border-radius: inherit`, `--dark` + backdrop). Document flow — no `100svh` / wheel traps. Product-page `MarketIntelligenceConsole` (`.mi-*`) unchanged. |
| Two-track (broker / B2B) | `landing/TwoTrack.tsx` `#platform-tracks` | **Equal-height identity cards 2026-08-25** + **global button contract:** id CTAs use shared `--primary` / `var(--radius)` / lift hover (partner no longer slate fill). ≥1024 stage stretch; id card `flex: 1` + CTA `margin-top: auto`. Anatomy: icon LEFT + eyebrow/title/body/pill→rounded CTA RIGHT. Consoles / `--tt-desk-h` unchanged. Intro L/R. Desks `--tt-desk-h: clamp(280px, 52svh, 420px)`. Broker → Explore products; B2B → SeatsDeal™. |
| Buttons / CTAs (site-wide) | `styles.css` global contract + `ui/button.tsx` | **2026-08-25:** one solid recipe (`--primary`, `--primary-foreground`, `var(--radius)`, 44px, lift hover) for Nav / Hero / FinalCTA / DemoModal submit / TwoTrack / ProcessBento CTA / FeatureOrbit Explore / NetworkConstellation. Outline shares radius + motion. Console `.smt-btn-*` keep compact size, same color/hover. |)
| ProcessBento (the problem) | `landing/Processbento.tsx` `#partner-process` | `.process-bento-*` / `.bento-*` in `styles.css`. **Reference rebuild 2026-08-25 (three bands):** (1) Top split — left eyebrow **The ticketing challenge** (sentence case, mint) + H2 **Your ticket operation** / mint **shouldn't live in separate systems.** + qualitative lead + mint **pill** callout (Link2 + “Keep the systems you already run. **Connect everything through SeatsBrokers.**”); right white card is a **systems diagram** (Your systems → SeatsBrokers engine mint panel → Your channels) with lucide chips and in-view dashed-arrow hop (no page-scroll trap). (2) Centered **One connected workflow.** + live **01–07** vertical step cards (Discover→Settle). (3) 4-up proof bar (inventory layer / keep systems / real-time sync / built for brokers) + footer Book a Demo CTA (`process-bento-cta-link` global button + `openDemoModal`). **Live flow kept:** `ProcessFlow` auto-advances `0→6` while in view (`useInView` `once: false`, 0.16); packet hop on N→N+1 join (~2100ms); Settle dwells then wraps; active mint border + wash; done priors; click jump; hover/press pause + 450ms resume; `prefers-reduced-motion` freeze; mobile scroll-into-view. Qualitative copy only. **Preserved (commented):** 7-capability feature card, prior two-col header + verbs, live `ProcessRail`, support line, five illustration bento cards + all `*Illustration` helpers. Older `.process-bento-rail*` / `.process-bento-header*` / `.process-bento-features*` CSS kept; new diagram/proof/callout-pill rules append and win. |
| How it works | `landing/HowItWorks.tsx` | `.how-it-*` — `section-curve-sticky`; left cards scroll, right visual `sticky`. **Seven numbered stages 01–07** on the official spine (`workflowStages`, same as ProcessBento / `/platform` / `/products`): 01 Discover SeatsIntel™ · 02 Source SeatsSource™ · 03 Price SeatsPulse™ · 04 Connect SeatsLink™ · 05 Distribute SeatsMarket™ · 06 Sell SeatsDeal™ · 07 Settle SeatsFunds™. Card = index + stage + product pill + module tagline + one sentence + **Explore {Product}™** `SiteLink` (`min-h-11`); bullets/proof strip removed. Right column pairs one visual per stage: `product-analytics-dashboard.png` (marketplace search), `add_listings_sb.png`, `product-events-browser.jpg` (market view), `product-market-insight-api.png` (API docs), `my_listings.png`, `dashboard.png` (sales), and a static light `.how-it-desk-*` settlement desk for SeatsFunds™ (no screenshot exists; illustrative £ rows, `data-kind="desk"` stretches to the shot row height). Active step comes from `useActiveStep` — it measures the card centers against the viewport middle, so the step count is not baked in. Desktop `.how-it-step` slot `min-height: 46svh` + `.how-it-sticky-pane` `calc(100svh - 6rem)` (svh, not dvh); mobile stacks shot-above-card at `py-6` and hides the desk Rail column under 480px. Stats chips stay qualitative (Global / Tracked / AI / In sync) — no `12K+` or `<200ms`. Close band is the required **One workflow. One inventory layer. Multiple sales channels.** + Become a Seller / Explore the platform (not a second FinalCTA). |
| Partner product showcase | `landing/PartnerProductShowcase.tsx` | `.partner-*` — five product tabs; copy + shots overlay the tallest variant (no `key=` remount) so auto-advance does not shift neighbors |
| Platform toolkit / Global Distribution | `landing/Toolkitshowcase.tsx` | `#platform-toolkit` Section 5 **light** (`.toolkit.toolkit-light` `bg-surface` — do **not** restore dark shell). **Connect Once. Reach More Buyers.** (`Reach` is the green accent). Desktop two-col **stretch**: left copy + flowchart (YOUR INVENTORY → SeatsBrokers lockup `src/assets/logo-2.png` on a **white** hub node, original colors, **no invert/filter** → 8-destination fan-out + shield footer) + right one static Ready `ConsoleShell` that fills the left-column height (table/settings grow; no inner scroll). **No partner logo strip.** Qualitative claims; CTAs Explore SeatsMarket™ + Become a Seller. Document flow `overflow: hidden` both axes. Mobile/tablet stack (no equal-height). Dest 2-col / 4-col from 640. No `100svh` / 340vh pin |
| Who we serve (sticky consoles) | `landing/StickyScrollShowcase.tsx` | `#who-its-for` deep sticky consoles (workflow titles + mini desks). Brief §9 role grid lives in `JourneyNumbers` (`#journey-numbers`) — do not duplicate four identical audience cards here. `.sss-*` + `StickyScrollConsoles.tsx`; right column swaps 4 unique light `ConsoleShell` mini-consoles. Shared `--sss-console-h`. Corner shell: `section-curve-sticky` + inner `.sss-shell` (`overflow: hidden`, `border-radius: inherit`, `--surface` + backdrop). Section itself is `overflow: visible` (sticky consoles need it) with no `overflow-x-clip` / no full-bleed `bg-surface`. Stacked console-above-copy below `lg`. `prefers-reduced-motion` freezes ticks. |
| Seller / travel tools | `landing/ToolsGrid.tsx` | SellerTools (`#sellers`, `.sto-*`): engine/console stays in the diagram center. Desktop diagram is **2-row with spanning rails**: Broker systems and stacked right (Connected marketplaces / Global buyers) **span both rows**; engine is center row 1; **01–06 sellerSteps** sit in **center column row 2** — the well under the engine between the side cards (not a full-width row after the tall 3-col). Cluster is **5+1** from `≥1280px` (`repeat(5, minmax(0, 1fr))`); **4+2** at `1024–1279` (`repeat(4, minmax(0, 1fr))`) because five overflowed that well. `grid-template-rows: auto 1fr` + `row-gap: 0.55rem` so process tucks under the engine. `.sto-board` gap `0`. Compact step type so copy is not clipped. `StoProcessFlow` hops 01→05 (or 01→04) then wraps to the next row (no six-across). `900–1023` is 5+1 stacked under the diagram; below 900 CSS arrows / stack. Side-rail hops stay on the engine gutters (sys rows no longer 1fr-stretch into the well). Packets run continuously in view (follow packet at half-duration; no hover-hold; `prefers-reduced-motion` freezes). Connected marketplaces names are **five stacked list rows** (one marketplace per row, same icon-row card language as Broker systems — not a horizontal strip). Live names: SeatPick, Hello Tickets, Stubhub, 1BoxOffice, Seatpin (no ™ on the row, matching the other four; Seatpin™ only in the fill sentence via `modules.link.name`). All five share `--primary` border + inset ring, 10% primary wash (same as active `.sto-mod` / `.sto-sys-item`), heavier type, sentence-case Live stamp; wash/dot pulse while `.sto-board[data-live]` (`prefers-reduced-motion` freezes). Generic rails Direct API / Partner feed / File drop / Webhook out and `API–feed` / `Drop–hook` ticks are gone. Leftover rail height below the five rows is typography (“16 connected marketplaces” + listings-out-through-Seatpin™ line), not an empty well. SellerTools copy uses **16** connected marketplaces (not 32). Side columns share `--sto-rail` width and stretch with the process well; leftover height is compact feed/status, not empty wells. Active module/connect/data use `--primary` wash. No logo, no cream. TravelTools (`#travel`, `.tpa-section`) is a quote-desk viewport: compact SeatsDeal™ intro + reserved `tpa-rail` row, then `.tpa-boards` — left Live `SeatMapTicketsConsole`, right `.tpa-side` (caption + 2×2 `.tpa-minis` + status strip). Column split is `1.22fr / 0.78fr` at `≥1920`; laptop `900–1919` gives minis more `fr` (`1.06/0.94` → `1.12/0.88` → `1.16/0.84`). Mini titles wrap 2 lines (no ellipsis). Right column is `.tpa-side`: compact pipeline caption (Pipeline 01–04, Select → Quote → Margin → Share, current auto-run step) above the 2×2 and a status strip (tickets held / quote value / margin / channels + SeatsDeal™ legend) below — leftover Live-column height is occupied by that chrome, not `place-content: center`. All four `.tpa-mini` share one height at each breakpoint (reserved 2-line title + 2-line note + board: meta, dial, 4 rows, foot + metric; `grid-template-rows: 1fr 1fr` at `≥480px`). Extra stretch goes into the four row cells, not a hollow well. Notes wrap 2 reserved lines (copy written to fit; no mid-sentence ellipsis). Laptop `1024–1919` keeps `.tpa-fit-zoom` `width: 100%` (do not inflate by `1/0.85` — that clipped the right minis). Desktop/laptop `≥900px` locks **`height` / `max-height` / `min-height: 100svh`** (not `dvh`) **and** keeps zero overlap: density is trimmed to fit (8 listing rows filling the table well, 3 mini rows, filters closed, 1-line titles/notes, side-steps hidden — pipeline stays on the rail). Mini rows use reserved one-line `min-height` + gap so labels never paint through each other. Map chrome stacks map → hint → share actions (44px CTAs) → wrapping `.smt-legend` on its own row (`.smt-desk-chrome`). Overflow stays `visible` (no section overflow-x clip / wheel trap). Table/map are overflow-x only. `<900px` stacks (1×4 under 480px), stays non-overlapping, and may exceed 100svh on phones. Available Ticket Listings is **8 rows** (`SEAT_MAP_LISTING_ROWS` in `seat-map-tickets-data.ts` + render slice; 4-row cap lifted so the table well fills; auto-run still picks L-1001 / L-1004 / L-1006 / L-1007). Table is `overflow-x: auto; overflow-y: hidden` only. No nested overflow-y-auto (same as HowItWorks: wheel over Live console / map / listings / minis / rail scrolls the page). Do not put `overflow-x-clip` on `.tpa-section` (that computes overflow-y to auto and traps wheel). `<900px` stacks rail → live → caption → 2×2 → status (1×4 under 480px). Desk fill is scoped `.tpa-section .smt-*` only. Remaining overflow-x inherits the global `--primary` custom scrollbar; no scoped TravelTools colors. `% Margin` popover (`smt-margin-pop`) is absolute under `.smt-list-head` (`right: 0`, `width: min(17rem, 100%)`). Function/data unchanged (`smt-*`, `seat-map-tickets-data.ts`): select → Quote → % Margin Apply → Copy / Copy Map / Clear / Download PDF. |
| Quote desk auto-run loop / mini consoles | `autoFrames` + `pipeline` in `landing/SeatMapTicketsConsole.tsx` | Declarative frame snapshots (stage, holds, picks, lines, margin %, channels) driven by one `setTimeout` effect, gated on `useInView` via `useSeatMapTickets({ active })`. `desk.pipeline` feeds the `tpa-*` stage rail and the four mini consoles in `ToolsGrid.tsx`. Edit timings/copy in `autoFrames` only — never push desk state from the cards. `takeControl` / `resumeAuto` handle manual override; share bar is always mounted so heights stay locked. |
| Who SeatsBrokers is for (§9) | `landing/JourneyNumbers.tsx` | `#journey-numbers` — **Built for Professional Ticket Businesses.** Four professional audiences (**Professional Ticket Brokers** / Ticket Resellers / Ticket Suppliers / Technology-Driven Ticket Businesses). Desktop in-view autoplay slide stack; mobile card stack. No unverified counts; travel/concierge not primary. StickyScroll keeps deep consoles. `.journey-num*`. Content-height; `prefers-reduced-motion` freezes. |
| Experience (§10) | `Stats.tsx` → `stats/StatsAccentLight.tsx` | `#network-stats` — **Built by Ticketing People, for Ticketing People** + three body paragraphs + four qualitative proof cards (30+ years ticketing experience / Global ticket distribution / Built specifically for B2B ticketing / For ticketing people). **Two-col header 2026-08-24:** `.stats-wallet-header` at `≥1024px` is the same 1.05fr / 0.95fr split as ProcessBento (eyebrow + H2 left; body right). Stacked below 1024. Wallet shuffle (`.stats-wallet-*`). Ledger/glow unmounted. No 10K+/32/165. |

## Product pages

| Request | Primary | Related |
|---|---|---|
| `/products` Seven products grid | `ProductsEcosystem.tsx` | `products-page-data.ts` `productCards`; `.prd-eco .prd-eco-grid` in `styles.css`. Seven cards: 1-col &lt;640, 2-col 640–1023, **4-col ≥1024**. **4+3 desktop 2026-08-24:** at ≥1024 flex-wrap + `justify-content: center` with locked **one-quarter** card widths — 01–04 row 1, 05–07 centered row 2 (not 3+3+1). At 640–1023 stays 2-col with centered incomplete last row. Mobile single column unchanged. `.prd-cap-*` and other product grids untouched. |
| Product URLs (Phase 2–4) | `src/content/site.ts` `productHrefs` + `src/routes/products/` | Overview `/products` unchanged. **Seven `/products/seats*` pages keep the brief skeleton** via `ProductStoryPage` **slots**: unique earlier heroes + Problem `SplitPanel` + How `WorkflowSteps` + **remounted live consoles/desks** + `ProductCapabilityBoard` + optional extra board + `ProductIntegrationChain` + Book a Demo / Become a Seller. `showFinalCta={false}`. Heroes (all Book a Demo + Become a Seller, `.page-cta-row` 44px): Intel `EventIntelHero`, Market `MarketplaceHero`, Deal `TravelPartnersHero`, Source `SourceHero` + `SourceConsoleWall` (`srh-*`, 6-event catalog + 4 layers + 5-row inventory sheet), Pulse `PulseHero` + `PulseConsoleWall` (`plh-*`, 6 signals + 3-col £247/£285/£265 + notes + Accept/Hold/Dismiss — no fake %), Link `LinkHero` + `LinkConsoleWall` (`lkh-*`, 6-row POS queue + 5-stage 1fr pipe + 5 stack chips), Funds `FundsHero` + `FundsConsoleWall` (`fnh-*`, 4 stages + Standard·USDT rails + 5-row settlement queue). Rooms fill Intel/Market clamp (header/body/footer stretch; density append after `srh-*`/`plh-*`/`lkh-*`/`fnh-*`). Copy in `*-hero-data.ts`. How desks unchanged. Extra capabilities: `EventCategoryBoard` / `MarketplaceCapabilityBoard`. PartnerTerms still unmounted. Generic channels only on Market minis/board. **Current chain pill 2026-08-22:** `.prd-chain-node[data-current="true"]` fill is append-only `oklab` + `--primary-soft` (oklch `--primary` + white hue-drifted to pink). Border/text stay `--primary`. **Platform integration chain narrow screens 2026-08-24:** `ProductIntegrationChain` (`.prd-chain-*`) at ≤1023px is horizontal snap-scroll (nowrap, hidden scrollbar, `overflow-x: clip` on section) — same pattern as `.process-bento-rail`; keeps arrow+pill flow on one line. Desktop ≥1024px flex-wrap unchanged. |
| `/become-a-seller` | `src/routes/become-a-seller.tsx` | `SellerHero` + `SellerApplyWall` (dark `bh-hero` split, `.slr-*`, Apply → Review → Access 3-row pipe + 5-desk queue + 3 review chips fill the clamp) + 6-benefit FeatureGrid (**3×2 `prd-cap-*` cards**) + `BrokerOnboardingSection` + compact `SellerApplicationForm` (`#apply`, `.saf-*`, desktop ~100svh). Copy: `seller-hero-data.ts` + `seller-application-data.ts`. FinalCTA off — the form is the close. `PageHero` unused. |
| `/brokers` composition | `src/routes/brokers.tsx` redirects to `/become-a-seller` | Catalog / inventory / AI / payments consoles remounted on `/products/seatssource`, `/products/seatspulse`, `/products/seatsfunds` How slots |
| Broker onboarding / company setup | `BrokerOnboardingSection.tsx` | `broker-onboarding-data.ts`, `.bon-*`. Six-part rebuild + layout/UX: region strip + comparison table (4-col, in-card `overflow-x` on small screens) + **one-open** six-phase accordion (inactive = `01 Apply` row; active expands We handle / You provide / region) + compact `ConsoleShell` auto-run (dense icon+label rail, 3-line ledger, auto height; sticky only ≥1024px **and** ≥1100px height) + partners + checklist + FAQ. Region tab drives parts 3–5. Not a LiveConsole variant. |
| Brokers hero wall | `BrokersHero.tsx` + `BrokersConsoleWall.tsx` | `broker-hero-data.ts`, `.bh-*` |
| Brokers Live Console desk fill | `LiveConsole.tsx` (`.lc-desk-fill`) | Stage tilt wrap is `width/max-width: 100%` at 375 / 768 / 1024 / 1440 / 1512 (was `max-width: 46rem` / `40rem` leaving a well in the 1.28fr column). AI desk: empty `.ap-hero` sibling removed; rec + guards + confidence gauge fill the 2-col. Extra columns are real panels, not flex-grow voids. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` untouched |
| Event catalog / inventory | `EventCatalogSection.tsx` / `InventoryManagementSection.tsx` | matching `*Console.tsx`. Catalog is document flow: `.ec-section` / console `height: auto`, `overflow: visible`. No `100svh` / locked `.lc-screen` clamp, no inner `overflow-y: auto` + `overscroll-behavior: contain`, no `overflow-x: clip` on `.ec-console-wrap` (that computes overflow-y auto and traps wheel). Same class as Toolkitshowcase’s old svh lock. No 340vh pin. Wheel over catalog/console scrolls the page. Event browser list is `events.slice(0, 5)` in `EventCatalogConsole.tsx` (`browserEvents`); `.ec-catalog-queue` reserves 5 row slots (`--ec-queue-row` / `--ec-queue-gap`, overflow visible). Copy/£ unchanged. `.sto-*` / `.tpa-*` / `.mps-*` / `.toolkit-*` untouched |
| Market intelligence overlap/layout (product pages) | `MarketIntelligenceConsole.tsx` | `.mi-*` in `styles.css`. Homepage §7 uses `landing/MarketIntelligence.tsx` + `.mihp-*` — do not conflate. |
| AI pricing console (brokers) | `AiPredictionsConsole.tsx` | LiveConsole `aiPredictions` |
| POS console | `PosConsole.tsx` | **built, not mounted** in `brokers.tsx` |
| Payments dashboard | `PaymentInfrastructureSection.tsx` | not LiveConsole |
| USDT / crypto payouts desk | `CryptoPayoutConsole.tsx` | LiveConsole `cryptoPayouts`, `crypto-payout-data.ts`, `.sfp-*`. Wallet / on-chain USDT hero; qualitative Standard switch; no ladder or sale maths. Payout track: two stacked cards size to content (`overflow: visible`, no `height: 100%` / 7.5rem min-height clip) |
| `/travel-partners` | redirects to `/products/seatsdeal` | Body lives on `src/routes/products/seatsdeal.tsx`. `pages/travel/`, `docs/travel-partners-page-plan.md`. **2026-08-24:** How slot leads with homepage `TravelTools` (SeatsDeal™ select → margin → quote → share desk), then inventory search / showcase / quotation / orders consoles. |
| Travel hero dashboard | `TravelConsoleWall.tsx` | `travel-hero-data.ts`, `.tpd-*` |
| Inventory search console | `InventorySearchConsole.tsx` | TravelLiveConsole `inventorySearch` |
| Search zoom cards | `SearchFilterShowcase.tsx` | `.sf-*` |
| Order & delivery desk | `PartnerOrdersConsole.tsx` | `.po-*` |
| Partner terms desk | `PartnerTermsSection.tsx` | `.pt-*` |
| `/marketplace-connectivity` | redirects to `/products/seatsmarket` | Body lives on `src/routes/products/seatsmarket.tsx`. `pages/marketplace/`, `docs/marketplace-page-plan.md` |
| Marketplace hero mosaic | `MarketplaceConsoleWall.tsx` | `.mkh-*` (fixed height `.mkh-room`) |
| Channel / listing / price consoles | `ChannelStatusConsole.tsx`, `ListingDistributionConsole.tsx`, `PriceConflictConsole.tsx` | `.ch-` `.ld-` `.pc-` |
| Capability board (was FeatureGrid) | `MarketplaceCapabilityBoard.tsx` | `.mcb-*` |
| `/event-intelligence` | redirects to `/products/seatsintel` | Body lives on `src/routes/products/seatsintel.tsx`. `pages/event-intelligence/`, `docs/event-intelligence-page-plan.md` |
| Event-intel hero AI lens | `EventIntelConsoleWall.tsx` | `.eih-*` |
| Category board (was FeatureGrid) | `EventCategoryBoard.tsx` | `.ecb-*` — qualitative coverage (Global catalog / Peak·High·Steady demand). No event-count totals. |
| Radar / demand / forecast / venue | `EventRadarConsole.tsx`, `DemandSignalConsole.tsx`, `ForecastConsole.tsx`, `VenueIntelligenceSection.tsx` | `.er-` `.ds-` `.fc-` `.vi-` `.vm-` |
| `/api` | `src/routes/api.tsx` | Developer page (not SeatsLink™). `pages/api/`, `api-hero-data.ts` `apiHeroCopy` + `apiInfra` → `ApiInfraBoard` (not FeatureGrid), `docs/api-page-plan.md` |
| API docs-console hero | `pages/api/` hero + wall | `.apidoc-*` |
| API infrastructure (was FeatureOrbitGrid) | `pages/api/ApiInfraBoard.tsx` | `apiInfra` in `api-hero-data.ts`; light 3×2 hairline cards + static Ready `ConsoleShell`; CSS `.api-infra-*` append only. Do not restyle `.fg-*` |
| Auth / webhook consoles | API live consoles | `.apk-*` `.whk-*` |
| `/about` | `src/routes/about.tsx` | Short commercial brief: AboutHero → AboutMission → AboutClose. PageShell FinalCTA stays. Detailed remount (Knowledge → Future) reverted by user request; chapter files kept. `pages/about/`, `docs/about-page-plan.md` |
| About hero intelligence core | `AboutHero.tsx` + `AboutJourneyWall.tsx` | `about-page-data.ts` (`aboutHeroCopy` already matches brief title + two paragraphs; CTAs Become a Seller / Book a Demo / Talk to our team). Dark `bh-hero` + `AboutJourneyWall` (`.abt-*`, not office atlas). Do not gut. |
| About mission (after hero) | `AboutChapters.tsx` `AboutMission` | `aboutMissionCopy`: H2 **Our Mission**, intro kept verbatim, SeatsGroup `lead` is infrastructure and distribution solutions + “We want to help businesses move from:” + `aboutTransforms`. Eyebrow unused. |
| About close | `AboutClose.tsx` | Pillars + Become a Seller / Book a Demo / Talk to our team. FinalCTA still via PageShell. |
| About unmounted chapters | `AboutChapters.tsx`, `AboutOverview.tsx`, `AboutLiveConsole.tsx`, `AboutPrinciples.tsx`, `AboutJourney.tsx` | Knowledge, Vision, Capabilities, Overview, LiveConsole, Audiences, Principles, Stack, Journey, Building, Future — files on disk, not mounted. 2026-08-22 full-width `.abt-*` tail override removed; older hero/mission/close `.abt-*` kept. |
| About pipeline console | `AboutLiveConsole.tsx` + `AboutOpsConsole.tsx` | Collect→Act pipeline, `.abt-ops-*` `.abt-pipe-*`. Built, unmounted. |
| `/book-demo` | `src/routes/book-demo.tsx` | `pages/book-demo/`, `book-demo-data.ts`, `.bdm-*` `.bds-*` |
| What you will see (was FeatureGrid) | `DemoSessionBoard.tsx` | `.bds-*` |
| `/platform` | `src/routes/platform.tsx` | Operating map (not a feature dump). Hero: `bh-hero` + dark alive `ConsoleShell` Discover→Pay & settle spine (`PlatformStackWall`, `.plt-os-*`). Copy uses **platform / workflow** — not “technology ecosystem.” Body: `#operating-stack` uses HowItWorks desktop geometry — `section-curve-sticky` (no overflow hidden), Tailwind `lg:grid-cols-2` + `minmax(0,1fr)` so the sticky ConsoleShell desk sits beside the cards. **Sticky fix 2026-08-22:** override `.plt-flow-grid { align-items: start }` → `stretch` so `.plt-flow-sticky` is as tall as the 7 cards; pane stays `position: sticky; top: 6rem; height: calc(100svh - 6rem)`; steps `min-height: 46svh`. Cards `w-full`; In/Out stack shares the paragraph left edge. Compact `PlatformDesks` (`.plt-mini-*`) + Explore CTAs. Supporting statement in hero subhead + flow intro. WorkflowSteps kept. Alignment patch append `.plt-flow-*`. |
| `/contact` | `src/routes/contact.tsx` | `pages/contact/ContactHero.tsx`, `.ct-*`; form still `ContactForm` in PageSections |
| `/integrations` | `src/routes/integrations.tsx` | Connect map (`integrationsPage` in `products-page-data.ts`). Hero + 8-category board + 3 live consoles in `pages/integrations/` (`.int-*`). Hero right stage is a **dark** detailed `ConsoleShell` (`int-room`): POS / feeds / APIs / websites / ERP / payments — categories only, no partner logos/names; stats + pipe + table + Now/status rail; **not** a timestamp terminal log. Body `.int-console` desks stay as they are. CTAs `ctas.discussIntegration` (`/contact`), `ctas.exploreLink`, `ctas.viewApiDocs` via `.page-cta-row` (44px nowrap). **CSS restore 2026-08-22:** `.int-*` recovered, then 2026-08-22 dark-hero append scoped to `.int-room`. Board meta is flex+gap so status is `01` + `Live`, not `01Live`. **No FeatureOrbitGrid / FeatureGrid on this page.** Not the SeatsLink™ product page. |
| Redirect shells | `src/routes/{market-analytics,ai-pricing}.tsx` | Redirect → `/products/seatspulse` |
| `/faq` | `src/routes/faq.tsx` | `pages/faq/`, `faq-data.ts`, `.faq-*` |
| `/legal` | `src/routes/legal.tsx` | `pages/legal/`, `legal-data.ts`, `.legal-*`; full-width 3-col tabs; footer hashes `#privacy` `#terms` `#cookies` |

## Shared patterns

| Request | Primary | Related |
|---|---|---|
| PageHero / FeatureGrid / SplitPanel / WorkflowSteps / ApiCards / ContactForm | `pages/shared/PageSections.tsx` | `COMPONENT_MAP.md`. PageHero is **unmounted** (kept). FinalCTA + `/integrations` + `/products` + `/become-a-seller` heroes use `.page-cta-row` (flex wrap, 44px, nowrap pills, full-width ≤639px). **Defined in `styles.css` 2026-08-22** — it was previously a phantom class (CHANGE_MAP claimed CSS that did not exist), which let inline `<a>` wrap mid-label (stair-step outline, “documentation” outside the green fill). WorkflowSteps `.ws-flow-grid.ws-flow-grid--center` (`WorkflowInfraCanvas`) is flex-wrap + `justify-content: center` with locked card widths (1 / 2 / 3 / 5 per row at 0 / 640 / 768 / 1024). Incomplete last rows center — 7 cards paint 01–05 then 06–07 in the middle, not left under 01–02. Card design/copy/order/green marks unchanged. Homepage HowItWorks (`.how-it-*`) is a different component. FeatureOrbitGrid stacks readable cards ≤767px (infinity diagram stays ≥768px) — **not mounted on `/integrations`**, **not** on `/products/seats*` Capabilities (`ProductCapabilityBoard`, `.prd-cap-*`), and **not** on `/become-a-seller` Key benefits (FeatureGrid now uses the same `.prd-cap-*` 3×2 cards). |
| Contact / demo / seller form | `ContactForm` (contact), `DemoRequestForm` (book-demo page), `DemoModal` (chrome), `SellerApplicationForm` (become-a-seller, `.saf-*`) | Fields in `book-demo-data.ts` / `seller-application-data.ts`. Modal chips from `moduleList`. Hand-off: `src/lib/lead-handoff.ts` (`VITE_LEAD_WEBHOOK_URL` or mailto) |
| Workflow card hover shades | `WorkflowInfraCanvas.tsx` | `.wic-glass-*` (`#4be5ae59` bottom blob) |
| Section connector rail | `pages/brokers/SectionConnector.tsx` | — |
| Console bezel | `pages/brokers/ConsoleShell.tsx` | `.lc-*` |
| Tokens / new CSS | `src/styles.css` **append marked block** | `docs/DESIGN_SYSTEM.md` |
| Global custom scrollbar | `src/styles.css` (`--sb-thumb` / `--sb-track` block at end) | Thumb `--primary`; do not reintroduce per-section colors |

## Content & config

| Request | Primary | Related |
|---|---|---|
| Nav, footer, CTA labels, SEO | `src/content/site.ts` `pageMeta` + `seoHead(path, entry)` | Canonical origin `siteOrigin` (`https://seatsbrokers.com`). `public/sitemap.xml` (indexable URLs only — no redirect shells). `public/robots.txt` Sitemap line. Root fallback title in `__root.tsx` matches `pageMeta.home`. Product module names: `src/content/modules.ts` |
| Hero mini-card / dashboard data | `src/content/*-hero-data.ts` | matching Wall component |
| Brokers USDT payout desk data | `src/content/crypto-payout-data.ts` | `CryptoPayoutConsole.tsx`, LiveConsole `cryptoPayouts` — qualitative rails/path/ledger, no £ or USDT amounts |
| About copy / manifesto chapters | `src/content/about-page-data.ts` | `pages/about/` |
| Book-a-demo copy / form options | `src/content/book-demo-data.ts` | `pages/book-demo/` |
| Platform overview copy / operating spine | `src/content/platform-page-data.ts` | Hero copy + `platformSupport` (one **platform**, not technology ecosystem) + seven-stage `platformStackLayers` / `platformModules` (Discover → Pay & settle) → `pages/platform/` |
| shadcn primitive | `src/components/ui/` | `components.json` |
| Vite / SSR | `vite.config.ts`, `src/start.ts`, `src/server.ts` | do not duplicate Lovable plugins |

## Do not look here first

- `src/routeTree.gen.ts` — generated
- `src/components/ui/` — unless changing a primitive
- Whole `styles.css` — search the page prefix
- `README.md` — original landing brief; not the live IA
